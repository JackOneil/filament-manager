# Filament Manager — Implementation Backlog

> **Backlog & feature tracking** for the Filament Manager project.
> Entries are prioritized by criticality and grouped by category.
> **Bug audit #1:** 2026-06-07 — deep codebase audit (models, migrations, routes, templates, JS, security, messages). All 39 findings **fixed in v1.111.0**.
> **Bug audit #2:** 2026-06-07 — second deep audit (all files, compliance scan, security review). 39 new findings recorded below (BUG-500 through BUG-538).
> **Bug audit #3:** 2026-06-08 — third deep audit (routes, models, JS, templates, utils, auth, messages). 15 new findings recorded below (BUG-539 through BUG-553).
> **Bug audit #4:** 2026-06-09 — fourth deep audit (all files, code quality, dead code, SQLAlchemy patterns, log hygiene). 6 new findings recorded below (BUG-554 through BUG-559).
> **Bug audit #5:** 2026-06-09 — complete full-codebase audit: all Python, routes, templates, JS, tests, config/Docker. 30 new findings recorded below (BUG-560 through BUG-589).
> **Bug audit #6:** 2026-07-18 — sixth deep audit: all routes, models, migrations, utils, templates, JS, auth, backup, security. 28 new findings recorded below (BUG-700 through BUG-727).
> **Bug audit #7:** 2026-07-18 — seventh audit (external agent): cross-verified findings against current codebase, added verified missing findings (BUG-704 through BUG-711).

---

## 🔴 Critical (Immediate)

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| BUG-700 | `routes/bambu.py` — missing `flash` import causes NameError at runtime | `routes/bambu.py:16`: The `from flask import` line does NOT include `flash`, but `flash()` is called at lines 85, 93, 122, 129, 221. Any code path hitting these lines (invalid date filter in Bambu job list) raises `NameError: name 'flash' is not defined` → HTTP 500. **Fix:** Add `flash` to the flask import on line 16. | 🔴 Critical | XS (Trivial) | Fixed in v1.119.13 |
| BUG-701 | `routes/auth.py` — missing `translate` import causes NameError on user delete | `routes/auth.py:2`: `from utils import utc_now, safe_commit` — does NOT import `translate`. But `translate()` is called at lines 304, 307 (user delete endpoint). Deleting a user (self or last admin) raises `NameError: name 'translate' is not defined` → HTTP 500 instead of proper 400 error. **Fix:** Add `translate` to the utils import on line 2. | 🔴 Critical | XS (Trivial) | Fixed in v1.119.13 |
| BUG-702 | `utils/__init__.py:1550` — greedy regex `({.*})` in JSON-LD extraction breaks on multiple JSON objects | `utils/__init__.py:1550`: `pattern = re.compile(r'({.*})', re.DOTALL)` — the greedy `.*` matches from the FIRST `{` to the LAST `}` across the entire script content. If a page has multiple JSON objects or nested structures with `}...{`, the regex captures garbage spanning multiple objects. This breaks link preview metadata extraction for pages with complex inline scripts. **Fix:** Use non-greedy `({.*?})` or parse JSON boundaries properly with a stack-based approach. | 🔴 Critical | S (Small) | Fixed in v1.119.13 |
| BUG-703 | `routes/maintenance.py:452` — ICS comma escaping uses `\\:` instead of `\\,` (RFC 5545 violation) | `routes/maintenance.py:452`: `text.replace(',', '\\:')` — iCalendar RFC 5545 §3.3.11 requires commas to be escaped as `\\,` (backslash + comma). The code incorrectly escapes to `\\:` (backslash + colon), producing malformed ICS output. Calendar apps may fail to parse SUMMARY/DESCRIPTION fields containing commas. **Fix:** Change `'\\:'` to `'\\,'`. | 🔴 Critical | XS (Trivial) | Fixed in v1.119.13 |
| BUG-704 | `app.py:694`, `routes/backup.py:129` — `0 or 10` prevents unlimited backup retention | Both locations use `getattr(setting, 'backup_auto_keep_count', 10) or 10`. When the user sets `backup_auto_keep_count = 0` (meaning "keep all/unlimited"), `0 or 10` evaluates to `10`, silently overriding the user's intent. Old backups are always deleted even when unlimited retention is configured. **Fix:** Use explicit `None` check: `keep_count = getattr(setting, 'backup_auto_keep_count', None); if keep_count is None or keep_count < 0: keep_count = 10`. | 🔴 Critical | XS (Trivial) | Fixed in v1.119.13 |
| BUG-705 | `migrations.py:218` — SQLite-incompatible `ALTER COLUMN` syntax silently fails | `migrations.py:218`: `_safe_alter(app, "ALTER TABLE filament_undo_log ALTER COLUMN snapshot_data DROP NOT NULL")` — SQLite does NOT support `ALTER COLUMN`. The `_safe_alter()` catches the exception and logs it, but the NOT NULL constraint is never dropped. Legacy databases keep `snapshot_data` as NOT NULL, causing potential `IntegrityError` if `NULL` is attempted. **Fix:** Use SQLite-compatible table recreation pattern (similar to `_migrate_nullable_project_id()`) or wrap in dialect check. | 🔴 Critical | S (Small) | Fixed in v1.119.13 |
| BUG-560 | `.env` not in `.dockerignore` — SECRET_KEY and FERNET_KEY leak into production Docker image (CWE-522) | `/opt/git/filament/.dockerignore` — The `.env` file (containing `SECRET_KEY`, `FERNET_KEY`) is not excluded from the Docker build context. Dockerfile line 33 (`COPY . .`) copies it into the production image. Anyone with pull access to the image can extract secrets. This is a CWE-522 (Insufficiently Protected Credentials) violation. **Fix:** Added `.env`, `node_modules/`, `*.log` to `.dockerignore`. | 🔴 Critical | S (Small) | Fixed in v1.119.3 |
| BUG-592 | `run_migrations()` leaves idle-in-transaction connections — blocks auto-backup and DDL on PostgreSQL | `migrations.py:239-260`: The seed data section reads `Brand.query.first()` and `AppSetting.query.first()` without committing or rolling back when data already exists. On PostgreSQL, this leaves idle-in-transaction connections that hold shared locks on `brand` and `app_setting` tables. These locks block ALL `ALTER TABLE` operations (`_safe_alter()`) and any query reading those tables — including the auto-backup worker (`AppSetting.query.first()` to read backup config). Result: auto-backup silently stops (last backup: 2026-06-06), manual backup hangs. **Fix:** Added `db.session.rollback()` in `else` branches after seed data checks. Added `db.session.rollback()` at the start of `_safe_alter()` to release pending locks before DDL. | 🔴 Critical | S (Small) | Fixed in v1.118.1 |
| BUG-590 | `db.Numeric`/`float` type mismatch causes HTTP 500 on `/projects` and `/stats` — Decimal/Float arithmetic crash | `Filament.price`, `AppSetting.kwh_price`, `ProjectQuote.final_price` are `db.Numeric(10,2)` which returns Python `Decimal` objects. Arithmetic with `float` (e.g. `price / weight_total * grams`) raises `TypeError: unsupported operand type(s)`. Affects `utils/__init__.py:572,590,604,612,616,619,635` (`build_project_metrics`), `routes/stats.py:326,354,356,361,362,366` (profitability calculation), `routes/projects_helpers.py:75,97,104` (`_job_cost_parts`). Regression from BUG-423 (v1.116.0). **Fix:** Added `float()` casts at all arithmetic sites involving `price`, `kwh_price`, and `final_price`. | 🔴 Critical | S (Small) | Fixed in v1.116.1 |
| BUG-001 | `_migrate_nullable_project_id` CREATE TABLE missing columns — crash on legacy upgrade | `migrations.py:300-320`: The `CREATE TABLE project_file_new` in `_migrate_nullable_project_id()` was missing **`share_token`** and **`model_note`** — causing crash on legacy upgrade. **Fix:** Added both columns to the CREATE TABLE. | 🔴 Critical | S (Small) | Fixed in v1.108.2 |
| BUG-002 | Help panel `currentSection` detection broken — blueprint prefix mismatch | `help.js:837-839` compared `window.__helpEndpoint` (Flask `request.endpoint`, e.g. `"inventory.index"`) against short endpoint names — match **always failed**. **Fix:** Stripped blueprint prefix in `base.html:378` via `.split('.').pop()`. | 🔴 Critical | S (Small) | Fixed in v1.108.2 |
| BUG-003 | Missing `PRAGMA foreign_keys=ON` — all FK constraints silently ignored by SQLite | `app.py:106-114`: Connection event listener was missing `PRAGMA foreign_keys=ON`. **Fix:** Added the PRAGMA to the `set_sqlite_pragma` event listener. FK constraints now enforced at DB level. | 🔴 Critical | S (Small) | Fixed in v1.108.2 |
| BUG-004 | `WasteRecord.filament_id` — NOT NULL FK without `ondelete`, risking orphaned references | `models.py:600`: `filament_id` FK was missing `ondelete`. **Fix:** Added `ondelete='CASCADE'` to `filament_id`, `ondelete='SET NULL'` to `project_id` and `recorded_by_user_id`. Added `_migrate_waste_record_fk()` migration for legacy DBs. | 🔴 Critical | S (Small) | Fixed in v1.108.2 |
| BUG-400 | `_migrate_nullable_project_id()` column order mismatch — silent data corruption | `migrations.py:329-330`: The `CREATE TABLE project_file_new` defined columns in a different order than the model. `model_note` and `uploaded_by_user_id` were swapped in positional mapping. **Fix:** Corrected column order in CREATE TABLE to match model. | 🔴 Critical | M (Medium) | Fixed in v1.111.0 |
| BUG-401 | `_migrate_waste_record_fk()` leaks `PRAGMA foreign_keys=OFF` on failure | `migrations.py:369-413`: Line 369 sets `PRAGMA foreign_keys=OFF`. If any exception occurs, the `except` block did NOT restore `PRAGMA foreign_keys=ON`. **Fix:** Added PRAGMA restoration in the except block. | 🔴 Critical | S (Small) | Fixed in v1.111.0 |
| BUG-500 | UserSession export references non-existent column `last_activity` (should be `last_activity_at`) | `routes/backup_helpers.py:380`: `s.last_activity.isoformat()` — model defines `last_activity_at` (models.py:63). Export crashes with `AttributeError` if any UserSession rows exist. Import at `routes/backup.py:978` uses `last_activity=` keyword which also doesn't match. | 🔴 Critical | S (Small) | Fixed in v1.113.0 |
| BUG-501 | UserSession export references non-existent column `expires_at` | `routes/backup_helpers.py:381`: `s.expires_at.isoformat()` — `UserSession` model has NO `expires_at` column. Export crashes with `AttributeError`. Import at `routes/backup.py:979` creates `UserSession(expires_at=...)` which raises `TypeError`. | 🔴 Critical | S (Small) | Fixed in v1.113.0 |
| BUG-502 | `stats.html:726` — `{{ chart_data|safe }}` without `|tojson` — XSS via filament name | `stats.html:726`: `const statsData = {{ chart_data|safe }};` — `chart_data` is a Python dict output raw into `<script>`. If any filament name contains `</script>`, it breaks out of the script tag and injects arbitrary HTML. **Fix:** Use `{{ chart_data|tojson }}` (which auto-escapes `</` sequences). | 🔴 Critical | S (Small) | Fixed in v1.113.0 |

---

## 🟠 High Priority (Next Sprint)

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| BUG-591 | Makerworld link preview broken — Cloudflare 403 + Jina reader opt-in gate regression | `utils/__init__.py:1684-1737`: Makerworld (and other Cloudflare-protected sites) returned HTTP 403 when accessed with bare `User-Agent` headers, causing link previews to show no metadata. Two root causes: (1) `fetch_link_metadata()` headers lacked modern browser-like headers (`Sec-Ch-Ua`, `Sec-Fetch-*`, etc.) that Cloudflare requires to bypass bot detection. (2) BUG-512 (v1.114.2) introduced an opt-in gate on the Jina reader fallback (default: disabled), so when direct fetch failed, the secondary reader never ran. **Fix:** (a) Added `Sec-Ch-Ua`, `Sec-Fetch-Dest/Mode/Site/User`, `Upgrade-Insecure-Requests`, `Accept-Language` headers to `fetch_link_metadata()`; upgraded UA to Chrome 131. (b) Removed the `link_preview_reader_enabled` gate — reader fallback now always runs when direct metadata is incomplete. (c) Added `coverUrl` to image keys in `_extract_preview_from_json_payloads()` for Next.js `__NEXT_DATA__` payloads. | 🟠 High | S (Small) | Fixed in v1.117.1 |
| BUG-561 | 14 test classes missing `AUTH_REQUIRED_IN_TESTS` — HTTP endpoint tests run with auth bypassed, providing false confidence | Affected tests: `test_bambu.py` (3 classes), `test_bambu_extended.py`, `test_calculator.py`, `test_calculator_extended.py`, `test_maintenance.py`, `test_projects.py` (2 classes), `test_prusa_extended.py`, `test_settings_integration.py`, `test_settings.py`, `test_storage_history_pwa.py` (2 classes), `test_waste.py`. None set `'AUTH_REQUIRED_IN_TESTS': True` in their `create_app()` config. The `before_request` auth guard is fully disabled. All HTTP assertions in these files would pass even if the auth layer were completely broken, providing false confidence in endpoint security. **Fix:** Add `'AUTH_REQUIRED_IN_TESTS': True` to every test class's app config. ⚠️ **Note**: Simply adding the flag breaks 43 tests — each class also needs proper login/auth setup (User creation, session login). Requires test refactoring, not just config. | 🟠 High | M (Medium) | **Open** |
| BUG-562 | `docker-compose.yml` has no `ports:` mapping — app unreachable on localhost:5050 as documented in README | `docker-compose.yml` (entire file): The service exposes port 5000 internally (`EXPOSE` in Dockerfile) but there is no `ports: - "5050:5000"` mapping. The README at line 177 says the app is at `http://localhost:5050`, but this doesn't work. The app is only reachable via the `traefik_proxy` overlay network. **Fix:** Add `ports: - "5050:5000"` under `filament_app:` in docker-compose.yml, and add missing `HEALTHCHECK` to Dockerfile. | 🟠 High | S (Small) | Fixed in v1.119.14 |
| BUG-563 | `routes/stats.py:223` — PrusaLink print usage (`prusa_print` action type) excluded from statistics charts and daily usage | `routes/stats.py:223`: `if row.action_type in ('remove', 'bambu_print')` — the `prusa_print` action type (used by `log_movement` in `routes/prusa.py` for PrusaLink deductions) is missing. Also `bulk_delete` is missing. This means all PrusaLink filament deductions and bulk deletions are silently excluded from the usage daily chart, material breakdown bar, purchase recommendations, and forecast calculations. **Fix:** Add `'prusa_print'` and `'bulk_delete'` to the action type check tuple. | 🟠 High | S (Small) | Fixed in v1.119.14 (both lines already had all 4 action types — pre-existing fix) |
| BUG-564 | `static/js/markdown-editor.js:306` — Unsanitized `innerHTML` from user-selected DOM content creates stored XSS vector | `createTaskItemNode(innerHtml, checked)` at line 306 sets `text.innerHTML = innerHtml` where `innerHtml` comes from `rangeToInlineHtml(range)` (line 403). This calls `range.cloneContents()` on user-selected content in a contenteditable `<div>`. Pasted rich content (e.g. `<img onerror=alert(1)>`) is NOT sanitized. When the user creates a task checkbox while malicious content is selected, the unsanitized HTML is injected into the DOM and persisted in project comments. **Fix:** Strip HTML tags via temporary div + `textContent` before inserting. | 🟠 High | S (Small) | Fixed in v1.119.14 |
| BUG-565 | `auth.py:712` — `invalidate_all_user_sessions()` performs bulk DELETE but never commits, silently losing session invalidation | `auth.py:712-720`: The function calls `.delete(synchronize_session='fetch')` but never calls `safe_commit()`. The deletion rolls back on the next transaction. Compare with `invalidate_all_other_sessions()` (line 700) which correctly calls `safe_commit()`. Also, `routes/auth.py:345,445` call this function without committing. Role/permission changes that should force re-login are silently ignored. **Fix:** Add `safe_commit()` before `return deleted` in `invalidate_all_user_sessions()`, and ensure all callers handle potential commit failures. | 🟠 High | S (Small) | Fixed in v1.119.14 |
| BUG-706 | XSS via `\|e` filter in onclick JS contexts (19 locations across 8 templates) | Templates `index.html`, `_filament_cards.html`, `_filament_list_rows.html`, `_bambu_job_cards.html`, `prusa.html`, `overview.html`, `stats.html`, `filament_detail.html`, `users.html`: 19+ locations use `onclick="openReorderShop('{{ fil.name\|e }}', ...)"` or similar patterns. The HTML `\|e` filter escapes `'` to `&#39;`, but the HTML parser decodes `&#39;` back to `'` before JS execution — a filament name containing a single quote breaks out of the JS string context and enables XSS. **Fix:** Replace `'{{ var\|e }}'` with `{{ var\|tojson }}` (no surrounding quotes, `\|tojson` produces a valid JS string literal). | 🟠 High | M (Medium) | Fixed in v1.119.14 |
| BUG-707 | `utils/__init__.py:1840-1868` — SSRF bypass via alternative IP representations in `validate_printer_host` | `validate_printer_host()` only checks exact localhost strings (`'localhost', '127.0.0.1', '::1', '0.0.0.0'`) and `startswith('127.')`. It does NOT reject octal (`0177.0.0.1`), hex (`0x7f000001`), decimal integer (`2130706433`), or IPv4-mapped IPv6 (`[::ffff:127.0.0.1]`) representations that resolve to loopback. The hostname regex on line 1865 allows these formats through. **Fix:** Resolve hostname to IP via `socket.getaddrinfo()`, then check the resolved IP against private ranges using `ipaddress.ip_address().is_private`. | 🟠 High | S (Small) | Fixed in v1.119.14 |
| BUG-708 | `routes/auth.py:345` — Admin self-logout on profile save via `user_detail` | When an admin edits their own user profile via `/users/<id>` (where id is the admin's own ID), line 345 calls `invalidate_all_user_sessions(user)` which invalidates ALL sessions for that user — including the current session. The admin is immediately logged out after saving their own profile. **Fix:** Add a check `if user.id == get_current_user().id:` and use `invalidate_all_other_sessions()` instead of `invalidate_all_user_sessions()`. | 🟠 High | XS (Trivial) | Fixed in v1.119.14 |
| BUG-709 | `routes/prusa.py:174-180` — ORM mutation after `db.session.rollback()` on detached object | `routes/prusa.py`: After `safe_commit()` fails (line 172), the except block (line 174) calls `db.session.rollback()`, then mutates `printer.last_sync_at` and `printer.last_sync_status` (lines 176-177). After rollback, the `printer` ORM object is detached — accessing its attributes may raise `DetachedInstanceError`, and mutations are not tracked by the session. **Fix:** Re-query the printer object after rollback before mutating, or use `db.session.merge()`. | 🟠 High | S (Small) | Fixed in v1.119.14 |
| BUG-601 | `project_create.html` — Alpine.js JS syntax error from `|tojson` quote nesting + `$parent` undefined in `$watch` scope | `templates/project_create.html:59,141`: Two issues on the project creation page. (1) Line 59: `@click="form.name = {{ name|tojson }}"` — Jinja2's `\|tojson` outputs double-quoted JSON strings inside a double-quoted HTML attribute, creating `@click="form.name = "name""` which is a JS syntax error (unexpected token `}`). This breaks ALL Alpine initialization. (2) Line 141: `x-init="$watch('$parent.form.description', ...)"` — `$parent` magic is genuinely not available during `x-init` evaluation in Alpine v3.14.9. The IIFE-based workaround also fails. **Fix:** (a) Changed `@click` attribute from double to single quotes: `@click='form.name = {{ name|tojson }}'`. (b) Removed all `$parent`-based sync from `x-init` entirely. Replaced with DOM-based approach: `goToStep(4)` reads description from `textarea[name="description"]` before showing review step. `applyTemplate()` sets the textarea value via native setter + `input` event for Alpine reactivity. | 🔴 Critical | XS (X-Small) | Fixed in v1.119.5 |
| BUG-602 | `/filaments` returns HTTP 500 — `strptime()` argument type mismatch (PostgreSQL returns `datetime.date`, SQLite returns `str`) | `utils/__init__.py:466,491`: `collect_sparkline_data()` calls `datetime.strptime(date_str, '%Y-%m-%d').date()` but on PostgreSQL, `db.func.date()` returns `datetime.date` objects (not strings). The `strptime()` call fails with `TypeError: strptime() argument 1 must be str, not datetime.date`. This breaks the main filament inventory page completely. Regression from PostgreSQL support (BL-006/v1.118.0). **Fix:** Added `isinstance(date_str, datetime.date)` check before `strptime()` — when the value is already a date object, use it directly. Also widened the except clause to catch `TypeError`. (utils/__init__.py:466,491) | 🔴 Critical | XS (X-Small) | Fixed in v1.119.5 |
| BUG-593 | Waste page AJAX endpoints not mapped in `SECTION_BY_ENDPOINT` — all AJAX interactions return 403 Forbidden | `auth.py:57-213`: The waste page has 7 AJAX endpoints (`waste_records_partial`, `waste_add_ajax`, `waste_edit_ajax`, `waste_delete_ajax`, `waste_data_ajax`, `waste_upload_ajax`, `waste_delete_file_ajax`) that were never added to `SECTION_BY_ENDPOINT`. This causes all waste page AJAX interactions (filter pills, modal create/edit/delete, photo upload) to fail with HTTP 403. Additionally, `maintenance_data` and `project_template_data` AJAX endpoints were also unmapped. **Fix:** Added all 9 missing endpoints to `SECTION_BY_ENDPOINT` with correct section mappings. Also updated `HELP_SECTIONS` in `static/js/help.js` with the missing endpoints. (auth.py, static/js/help.js) | 🟠 High | S (Small) | Fixed in v1.119.1 |
| BUG-594 | Backup export/import missing 7 fields — data loss on restore | Comprehensive audit revealed 7 fields missing from the backup/restore cycle: `FilamentUndoLog.target_type`, `FilamentUndoLog.target_key` (🔴), `Project.share_token` (🔴), `User.last_login_at` (🟠), `AppSetting.link_preview_reader_enabled` (🟠), `BambuPrintJob.raw_payload` (🟠), `PrusaPrintJob.raw_payload` (🟠). **Fix:** Added all 7 missing fields to both export (`backup_helpers.py`) and import (`backup.py`) serialization. | 🔴 Critical | M (Medium) | Fixed in v1.119.2 |
| BUG-595 | Heatmap legend shows unresolved `{{T:less}}`/`{{T:more}}` placeholders | `static/js/enhancements.js:257-264`: The `init()` function tries to replace `{{T:less}}`/`{{T:more}}` placeholders in `.enh-heatmap-legend` elements BEFORE `initHeatmaps()` creates them. Result: placeholders remain unresolved on the page. **Fix:** Moved i18n label resolution into `initHeatmaps()` where the legend HTML is generated. | 🟡 Medium | XS (X-Small) | Fixed in v1.119.2 |
| BUG-600 | Markdown code block unreadable in light theme — `[&_code]:bg-gray-100` overrides dark `<pre>` background | `templates/_project_overview.html:6,307`, `templates/project_share.html:57`: All 3 rendered markdown description blocks used `[&_code]:bg-gray-100` to style inline code, but this also applied to `<code>` inside `<pre>` blocks. In light theme, the dark `<pre>` background (`bg-slate-900`) was overlaid by the light gray code background, rendering light text (`text-slate-100`) unreadable. **Fix:** Added `[&_pre_code]:bg-transparent` after `[&_code]:bg-gray-100` in all 3 locations to make code blocks inside `<pre>` have transparent background. | 🟡 Medium | XS (X-Small) | Fixed in v1.119.4 |
| BUG-005 | CSP nonce missing from all template-specific `<script>` blocks | 26 `<script>` blocks across 16 templates lacked nonce injection. **Fix:** Added `{% if csp_nonce %} nonce="{{ csp_nonce }}"{% endif %}` to all inline `<script>` blocks in v1.109.0. | 🟠 High | M (Medium) | Fixed in v1.109.0 |
| BUG-006 | Test `test_bambu_extended:test_job_delete_nonexistent_returns_404` asserts 302, not 404 | `test_bambu_extended.py:138-141`: Test name claimed 404 check but asserted 302. **Fix:** `bambu_job_delete` now returns proper `jsonify({'ok': False, 'error': 'not found'}), 404`; test asserts 404 with JSON body. | 🟠 High | S (Small) | Fixed in v1.108.2 |
| BUG-007 | Test `test_projects_extended:test_advance_status_full_flow` has zero assertions | `test_projects_extended.py:119-126`: Test method loops through statuses but makes no HTTP requests and performs no assertions. **Fix:** Added HTTP requests and final status assertion. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-008 | Test `test_stats_extended:test__project_usage_rows` has impossible-to-fail assertion | `test_stats_extended.py:177`: `self.assertGreaterEqual(len(rows), 0)` — `len()` always returns >= 0. **Fix:** Replaced with meaningful assertions checking project name presence. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-009 | Test `test_inventory_extended:test_delete_filament_cleans_project_references` contains `pass` instead of assertions | `test_inventory_extended.py:299-310`: The `with` block creates a dummy object and does nothing (`pass`). **Fix:** Creates real Project/ProjectFilament records and verifies cleanup. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-010 | Tour engine: hardcoded Czech tooltip text | `tour.js:707`: Close button `title="Zavřít"` hardcoded in Czech. **Fix:** Uses `this._t({cs:'Zavřít',en:'Close'})`. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-011 | `showToast` i18n fallback is dead code (identity replacement) | `app-shell.js:232`: The `replace()` callback `function(m){ return m; }` returns the matched text unchanged. **Fix:** Removed dead fallback. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-012 | Hardcoded strings `'Rows'`, `'Color'`, `'Resize'` not translatable | `dashboard.js:81,390,498,1056`: Four instances of hardcoded English strings. **Fix:** Made configurable via `window._dash*Title` globals set from template `t()` calls. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-013 | Duplicate global `openReorderShop` function | `inventory.js:71-74` shadows `window.openReorderShop` from `app-shell.js:186-188`. **Fix:** Removed duplicate from inventory.js. | 🟠 High | S (Small) | Fixed in v1.110.0 |
| BUG-402 | `Project.owner_user_id`/`created_by_user_id` missing `ondelete` | `models.py:283,285`: These two FK columns omitted `ondelete`. Deleting a user would raise `IntegrityError`. **Fix:** Added `ondelete='SET NULL'` to both columns. | 🟠 High | S (Small) | Fixed in v1.111.0 |
| BUG-403 | `ProjectFile.parent_file_id` FK: model=CASCADE, migration=SET NULL | `models.py:343` says `ondelete='CASCADE'` but `migrations.py:334` created the column with `ON DELETE SET NULL`. **Fix:** Changed migration to use `ON DELETE CASCADE`. | 🟠 High | S (Small) | Fixed in v1.111.0 |
| BUG-404 | Systemic `onsubmit="return confirm('...')"` JS injection risk (44 instances) | 44 `onsubmit` handlers across 22 templates interpolated translated strings directly into JS context. **Fix:** Replaced with `data-confirm` attribute pattern + global event handler. | 🟠 High | L (Large) | Fixed in v1.111.0 |
| BUG-405 | No rate limiting on login endpoint | No rate limiting, throttling, or lockout on `login` endpoints. **Fix:** Added in-memory rate limiter — 10 attempts per 5-minute window per IP. | 🟠 High | M (Medium) | Fixed in v1.111.0 |
| BUG-406 | No password policy enforcement | `hash_password()` uses scrypt but no minimum length/complexity check. **Fix:** Added `validate_password_strength()` — enforces 8–256 char length on registration, activation, and password change. | 🟠 High | S (Small) | Fixed in v1.111.0 |
| BUG-407 | Missing CSP nonce on `<script>` tags in base.html | `base.html`: 8 external `<script src="...">` tags lacked the `nonce` attribute. **Fix:** Added `{% if csp_nonce %} nonce="{{ csp_nonce }}"{% endif %}` to all external script tags. | 🟠 High | S (Small) | Fixed in v1.111.0 |
| BUG-503 | Unmapped endpoints bypass RBAC for all authenticated users | `auth.py:411-412`: When an endpoint has no entry in `SECTION_BY_ENDPOINT`, `ensure_endpoint_access()` returns `None` (allow). Any authenticated user — including regular users with no section permissions — can access unmapped endpoints. If a developer adds a new route and forgets to map it, it's wide open. **Fix:** Default to `abort(403)` for unmapped endpoints, or maintain an explicit `UNRESTRICTED_ENDPOINTS` set. | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-504 | `calculator_project.html` — 15+ hardcoded Czech strings (Rule 1 violation) | `calculator_project.html:21-272`: All user-visible text is hardcoded Czech — `'Kalkulace z projektu'`, `'Reálná data z tiskových jobů'`, `'Materiály projektu'`, `'Přepočítat'`, `'Uložit kalkulaci'`, `'Nastavení energie'`, etc. None use `{{ t("key") }}`. English users see Czech text. | 🟠 High | M (Medium) | Fixed in v1.113.0 |
| BUG-505 | `inventory.py:276` — Zero price/weight prevents filament creation (logic bug) | `routes/inventory.py:276`: `if not all([brand_id, color_id, material_id, weight_total, price is not None])` — Python's `all()` treats `0.0` as falsy. A filament with `price=0` (free) or `weight_total=0` cannot be created; the form silently redirects back to `/add`. **Fix:** Check `is None` explicitly for each field. | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-506 | `BambuPrintJob.deducted` and `BambuJobMaterial.deducted` missing `nullable=False` | `models.py:497,517`: Both use `db.Column(db.Boolean, default=False)` without `nullable=False`. Compare with `PrusaPrintJob.deducted` (line 555) which correctly has `nullable=False`. NULL values could cause `if job.deducted:` checks to silently pass. | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-507 | `ProjectFilament.is_used` missing `nullable=False` | `models.py:392`: `is_used = db.Column(db.Boolean, default=False)` — no `nullable=False`. Inconsistent with other boolean columns and could mask bugs when NULL is treated as falsy. | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-508 | ICS injection in maintenance calendar export — unsanitized user input | `routes/maintenance.py:457,464`: `rec.printer_name` and `rec.notes` are interpolated directly into iCalendar SUMMARY and DESCRIPTION fields without escaping. A newline in `notes` (e.g. `foo\r\nEND:VEVENT\r\nBEGIN:VEVENT...`) injects fake calendar events. **Fix:** Strip newlines and iCal-escape special characters (`;`, `,`, `\n`). | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-509 | No rate limiting on `/register` endpoint | `routes/auth.py:168-199`: Registration has no rate limiting or throttling. An attacker could create unlimited user accounts, filling the database. The first registered user becomes admin. **Fix:** Apply `_check_login_rate_limit` or a separate IP-based limiter. | 🟠 High | S (Small) | Fixed in v1.113.0 |
| BUG-510 | No session invalidation on role/permission change | `auth.py:320-331`: When an admin changes a user's role or section permissions, existing sessions are NOT invalidated. A user whose permissions were reduced retains access until their session expires (14 days). `ensure_endpoint_access` only checks session existence, not current permissions. **Fix:** Add `permissions_hash` or `role_changed_at` to `UserSession` and re-verify in `ensure_endpoint_access`. | 🟠 High | M (Medium) | Fixed in v1.113.0 |
| BUG-539 | `settings.html` — 2 remaining `confirm()` JS injection instances (BUG-404 pattern) | `templates/settings.html:1393`: `onclick="if(confirm('{{ t('backup_auto_trigger_confirm') }}')){ ... }"` and `settings.html:1464`: `onclick="return confirm('{{ t('import_confirm') }}');"` — translated strings embedded directly in `onclick` JS attributes. Same BUG-404 pattern supposedly fixed in v1.111.0, still present in 2 places. **Fix:** Converted both to `data-confirm` attribute pattern: backup trigger now button → hidden form with `data-confirm`, import form now has `data-confirm` on the form tag, `onclick` removed from button. | 🟠 High | S (Small) | Fixed in v1.114.1 |
| BUG-540 | `models_index.html:370` — BUG-404 pattern in Alpine `@submit.prevent` | `templates/models_index.html:370`: `@submit.prevent="if(confirm('{{ t('models_bulk_delete_confirm') }}')) submitBulk($el)"` — translated string embedded in Alpine JS attribute context. **Fix:** Replaced with `data-confirm-msg` attribute read via `$el.dataset.confirmMsg` in Alpine expression — avoids embedding translated string in JS context. | 🟠 High | S (Small) | Fixed in v1.114.1 |
| BUG-541 | `routes/projects.py` — `abort(401)` used instead of `abort(403)` in 6 file ownership checks | `routes/projects.py:584,601,613,627,644,656`: Six instances use `abort(401)` when `project_file.project_id != id`. HTTP 401 means "Unauthenticated" — the user IS authenticated. HTTP 403 ("Forbidden") is correct. **Fix:** Replaced all 6 `abort(401)` calls with `abort(403)`. | 🟠 High | S (Small) | Fixed in v1.114.1 |
| BUG-542 | `project_create` and `project_edit` allow empty `name` — triggers raw IntegrityError | `routes/projects.py:262` (`project_create`) and `routes/projects.py:433` (`project_edit`): `name = request.form.get('name', '').strip()` has no non-empty validation. **Fix:** Added `if not name: flash('project_name_required', 'error'); return redirect(...)` in both handlers. Added `project_name_required` i18n key to cs+en. | 🟠 High | S (Small) | Fixed in v1.114.1 |

---

## 🟡 Medium Priority (Upcoming)

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| BUG-566 | 10+ hardcoded `fetch()` paths in templates instead of `url_for()` — route changes silently break AJAX | Templates: `templates/index.html:922` (`/api/filaments-list`), `index.html:982` (`/filaments`), `index.html:1085` (`/use/`), `index.html:1100` (`/add_spool/`), `storage.html:1008` (`/storage/shelf/reorder`), `storage.html:1077` (`/storage/placement/`), `models_detail.html:942,974` (`/models/version/`), `models_index.html:550` (`/api/models-list`), `_project_overview.html:399` (`/projects/X/comments/Y/react`), `settings.html:1066` (`/prusa/printer`). These are hardcoded URL paths used in `fetch()` calls. If routes are refactored, these silently break. **Fix:** Use `url_for()` in Jinja2 to generate proper blueprint-prefixed URL paths for each `fetch()` call. | 🟡 Medium | M (Medium) | **Open** |
| BUG-567 | Multiple XSS vectors in templates via `x-text="'{{ t('key') }}'"` and `x-data` with unsanitized translation interpolation | Templates `_project_overview.html:73,292`, `_project_materials.html:17`, `project_detail.html:351-352`, `calculator.html:37,129`, `bambu.html:835,845` use patterns like `x-text="addOpen ? '{{ t('cancel') }}' : '{{ t('key') }}'"` or `x-data='{placeholder: "{{ t("key") }}"}'`. If a translated string contains a single quote, the JavaScript/HTML attribute context breaks, enabling XSS. **Fix:** Use `{{ t('key')|tojson }}` filter without manual single-quote wrapping in all Alpine attributes — the `|tojson` filter produces a properly-escaped JavaScript string. | 🟡 Medium | M (Medium) | **Open** |
| BUG-568 | Dockerfile runs as root without HEALTHCHECK — security and observability gaps | `/opt/git/filament/Dockerfile`: No `USER` directive (process runs as root — if compromised, attacker has full root access). No `HEALTHCHECK` instruction (Docker cannot detect a stuck/unhealthy app). **Fix:** Add `RUN addgroup --system app && adduser --system --ingroup app app`, adjust data directory ownership, add `USER app` before CMD, and add `HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 CMD curl --fail http://localhost:5000/ || exit 1`. | 🟡 Medium | M (Medium) | **Open** |
| BUG-569 | `requirements.txt` pins outdated Flask 3.0.2 and Werkzeug 3.0.1 with known CVEs, loose cryptography constraint allows major version jumps | `Werkzeug==3.0.1` (current 3.1.7) has known CVEs including CVE-2024-49766 (debugger RCE), CVE-2024-49767 (resource exhaustion). `cryptography>=42` permits auto-upgrade from 42.x to 48.x (current 48.0.0) — major version bumps may include breaking API changes. No lockfile for reproducible builds. **Fix:** Update to `Werkzeug>=3.0.1,<4`, narrow to `cryptography>=42.0,<46`, add `requirements.lock`. | 🟡 Medium | M (Medium) | **Open** |
| BUG-570 | pytest.ini + requirements.txt | pytest.ini:4 + requirements.txt | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-571 | `.dockerignore` missing `node_modules/`, `*.log`, `test_report.html` — build context bloat and potential data leak | Current `.dockerignore` only excludes `.git`, `__pycache__`, `.venv`, `.codex`, `.pytest_cache`, `.vscode`. Missing: `node_modules/` (sent to Docker daemon even though it's rebuilt inside), `*.log` (may contain stack traces/SQL/tokens), `test_report.html`, `README.md`, `CHANGELOG.md`. **Fix:** Add `/node_modules/`, `*.log`, `test_report.html`, `README.md`, `CHANGELOG.md` to `.dockerignore`. | 🟡 Medium | S (Small) | **Open** |
| BUG-572 | No `conftest.py` — 28 test classes duplicate identical `create_app({...})` boilerplate (~420 lines of waste) | Every test file in `tests/` repeats the same setup: `tempfile.mkdtemp`, `create_app({TESTING, SQLALCHEMY_DATABASE_URI, PROJECT_UPLOAD_FOLDER, WTF_CSRF_ENABLED, AUTH_REQUIRED_IN_TESTS})`, `test_client()`, `app_context()`. This is copied verbatim across 28 classes. A single `conftest.py` with `@pytest.fixture` would eliminate this. **Fix:** Create `tests/conftest.py` with shared fixtures, and refactor all test classes to use them. (Already logged as TD-004 but now quantified.) | 🟡 Medium | M (Medium) | **Open** |
| BUG-573 | nullable=False on status/estimated_print_time | models.py:307,460 | 🟡 Medium | S (Small) | Fixed in v1.119.15 (2/4 fixed)|
| BUG-574 | CSS selector CSS.escape() | dashboard.js:7 locations | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-575 | `routes/settings.py` — 30 instances of f-strings in `app.logger.debug()` calls | Lines 101,111,120,125,130,134,141,153,157,165,172,179,185,191,197,207,216,223,235,247,291,297,305,339,364,370,377,386,574,581 all use `app.logger.debug(f"...{var}...")` pattern. F-strings are evaluated eagerly even when DEBUG level is disabled. With `setting.debug_logging` controlling the level, this burns CPU on every settings save. **Fix:** Convert to lazy `%s` formatting: `app.logger.debug("... %s ...", var)`. | 🟡 Medium | M (Medium) | **Open** |
| BUG-576 | SQLAlchemy filter False→None | routes/models.py:175 | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-577 | `test_settings_integration.py:528-551` — `WasteReasonsTests` tests are misnamed, target wrong endpoint, and have vacuous assertions | Both test methods post to `backup_auto_settings` action (not waste reasons) and only assert `setting is not None` — a vacuous check since setting was guaranteed by setUp. Test names claim to test waste reasons but don't touch that feature at all. **Fix:** Either implement actual waste-reason CRUD assertions or remove the tests. | 🟡 Medium | S (Small) | **Open** |
| BUG-578 | `test_performance.py:101-134` — 7 flaky wall-clock timing assertions with < 2s thresholds | Seven test methods assert `elapsed < 2000` or `< 3000` milliseconds for operations like filament listing, stats page rendering, BMP280 data, etc. These thresholds are not generous enough for CI/container environments under CPU throttling. **Fix:** Increase thresholds to 5-10s, or use statistical averaging, or separate into a non-gating performance suite. | 🟡 Medium | S (Small) | **Open** |
| BUG-554 | SQLAlchemy filter False→None | routes/models.py:174 | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-555 | `stats.py:209` — `defaultdict(lambda: ...)` captures `_label_keys_snapshot` by reference, fragile under refactoring | `routes/stats.py:209-210`: `material_daily = defaultdict(lambda: {key: 0.0 for key in _label_keys_snapshot})`. Python lambda closures capture variables by reference, not by value. If `_label_keys_snapshot` is reassigned elsewhere, all existing `material_daily` entries would silently shift to the new reference. Currently works because the variable is only set once and never reassigned, but this is a brittle pattern. **Fix:** Capture by value at lambda creation time: `lambda keys=list(label_keys): {key: 0.0 for key in keys}` or use a dict comprehension on `label_keys`. | 🟡 Medium | XS (Trivial) | Fixed in v1.119.15 |
| BUG-556 | `routes/backup.py:462` — dead code: `imported_files_map = {}` assigned but never read | `routes/backup.py:462`: `imported_files_map = {}` is declared in the import section but never used anywhere in the function. This is dead code that adds clutter and could mislead future developers. **Fix:** Remove the dead assignment. | 🟡 Medium | XS (Trivial) | **Closed — false positive** (used at lines 524, 531) |
| BUG-557 | `routes/inventory.py:524` — `db.session.rollback()` in `except` block can mask the original exception if rollback itself fails | `routes/inventory.py:522-525`: `except Exception: db.session.rollback(); app.logger.exception("Undo action failed")`. If `db.session.rollback()` raises its own exception (e.g. closed connection), the rollback exception replaces the original exception silently, and `app.logger.exception()` is never called. Both the root cause and the log message are lost. **Fix:** Wrap `rollback()` in an inner `try/except` with `pass` before calling `app.logger.exception()`. | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-710 | `safe_commit()` return value ignored across 30+ locations — silent data loss on commit failure | `safe_commit()` (utils/__init__.py:106) returns `True` on success and `False` on failure. However, 30+ call sites across `routes/inventory.py` (13 calls), `routes/projects.py` (36 calls), and other route files do not check the return value. When a commit fails, `safe_commit()` rolls back, flashes an error, and returns `False` — but the caller continues as if the save succeeded (showing `success` flash, redirecting). Data changes are silently lost. **Fix:** Either change `safe_commit()` to raise on failure (breaking change), or add return-value checks at all call sites with `if not safe_commit(): return redirect(...)`. | 🟡 Medium | L (Large) | **Open** |
| BUG-711 | N+1 joinedload reactions | routes/projects.py:337 | 🟡 Medium | S (Small) | Fixed in v1.119.15 |
| BUG-100 | Missing `ondelete` on remaining FK columns (MovementHistory, BambuJobMaterial) | `models.py:157-159,516`: FK columns lack `ondelete`. **Fix:** Added `ondelete='SET NULL'` to all 4 remaining FK columns. | 🟡 Medium | M (Medium) | Fixed in v1.109.1 |
| BUG-101 | Filament FK columns (`brand_id`, `color_id`, `material_id`) have no `ondelete` | `models.py:125-127`: All three are `NOT NULL` with no cascade. **Fix:** Added `ondelete='RESTRICT'` to all three FK columns. | 🟡 Medium | S (Small) | Fixed in v1.109.0 |
| BUG-102 | Hardcoded user-visible strings in route error responses (20+ instances) | Multiple hardcoded English/Czech strings in error responses. **Fix:** Replaced with `abort()` or `translate()`. | 🟡 Medium | M (Medium) | Fixed in v1.109.0 |
| BUG-103 | Hardcoded user-visible text in templates (22+ instances) | Sidebar tooltips, menu labels, etc. hardcoded. **Fix:** Replaced all with `{{ t("key") }}`. | 🟡 Medium | L (Large) | Fixed in v1.109.0 |
| BUG-104 | Missing endpoint `bambu_auto_map_history` in help.js bambu section | `help.js:379`: Registered in SECTION_BY_ENDPOINT but absent from help.js. **Fix:** Added to endpoints array. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-105 | Missing endpoint `project_undo` in help.js projects section | `help.js:177`: Registered in SECTION_BY_ENDPOINT but absent from help.js. **Fix:** Added to endpoints array. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-106 | `ProjectFile.versions` ORM cascade contradicts FK | `models.py:355`: `cascade='all, delete-orphan'` vs FK `ON DELETE SET NULL`. **Fix:** Changed FK to `ON DELETE CASCADE`. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-107 | Missing `db.session.rollback()` in ~30 POST handlers | Many POST handlers call `db.session.commit()` without `try/except`. **Fix:** Added global `app.teardown_request` handler. | 🟡 Medium | M (Medium) | Fixed in v1.110.0 |
| BUG-108 | No HTTP endpoint test coverage for Prusa routes | 5 HTTP endpoints with zero test coverage. **Fix:** Added 23 tests. | 🟡 Medium | M (Medium) | Fixed in v1.110.0 |
| BUG-109 | Duplicate test in test_stats_extended | `test_stats_extended.py:209-211` identical to lines 188-190. **Fix:** Removed duplicate. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-110 | Dead code in test_bambu_extended._login_admin | `if False else None` pattern. **Fix:** Removed dead pattern. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-111 | `loadScript` caches rejected promises permanently | Failed script loads store rejected Promise in cache. **Fix:** Delete cache entry on failure. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-112 | `model_public_share` no test coverage | Listed in help.js but endpoint untested. **Fix:** Added 3 tests. | 🟡 Medium | S (Small) | Fixed in v1.110.0 |
| BUG-408 | Hardcoded English strings in JSON API error responses (20+ instances) | Error responses returned raw English strings. **Fix:** Replaced with `translate()` calls across bambu.py, projects.py, settings.py, prusa.py, models.py. | 🟡 Medium | M (Medium) | Fixed in v1.111.0 |
| BUG-409 | Hardcoded URL paths instead of `url_for()` (7 locations) | JS `fetch()` calls used absolute paths. **Fix:** Replaced with `url_for()` calls in overview.html, bambu.html, prusa.html. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-410 | Unsafe `|escape` in JS string context (XSS via filename) | `project_detail.html`: Filename interpolated via `|escape` into JS string. **Fix:** Changed to data-attribute pattern with `$el.dataset`. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-411 | `project_query.whereclause` accesses internal SQLAlchemy API | `auth.py:450`: Internal `.whereclause` attribute used. **Fix:** Rebuilt filter cleanly using `user_condition`. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-412 | `calculator.py:247` — unprotected `db.session.commit()` after log | Commit not wrapped in try/except. **Fix:** Wrapped in try/except with rollback. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-413 | Fernet encryption silently falls back to plaintext | `encrypt_token()` returned plaintext when FERNET_KEY missing. **Fix:** `encrypt_token()` now raises RuntimeError (except during testing/placeholders). | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-414 | Session invalidation on password change not enforced everywhere | `invalidate_all_other_sessions()` defined but unclear coverage. **Fix:** Password change route already calls it; confirmed via code audit. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-415 | messages.py: 2 keys missing from `en` dict | `printer_bambu` and `printer_prusa` existed only in `cs`. **Fix:** Added both to `en` dictionary. | 🟡 Medium | XS (Trivial) | Fixed in v1.111.0 |
| BUG-416 | messages.py: 4 translation errors | `overview_command_center_title` (cs: "Command center"), `maintenance_fault_resolved` (cs: "Resolved"), `markdown_editor_visual` (cs: "Visual"), `account_lang_cs` (en: "Čeština"). **Fix:** Corrected to proper translations. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-417 | Missing CSP nonce on standalone pages | `quote_export.html` and `models_share.html` had no CSP nonce. **Fix:** Added nonce to all `<script>` tags on standalone pages. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-418 | Raw `comment.body_html` rendered — potential XSS via markdown | `_project_overview.html:292`: `{{ comment.body_html }}` without safe filter. **Fix:** Added `|safe` filter (render_markdown produces sanitized HTML). | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-419 | JS string injection via translated strings in `x-text` | `project_detail.html:351`: JS string literal with unescaped translations. **Fix:** Replaced with two `x-show` spans. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-420 | `overview_user` missing from `SECTION_BY_ENDPOINT` — potential auth bypass | `overview_user` endpoint had no entry in `auth.py:SECTION_BY_ENDPOINT`. **Fix:** Added `'overview_user': SECTION_OVERVIEW`. | 🟡 Medium | S (Small) | Fixed in v1.111.0 |
| BUG-421 | 3 AJAX endpoints missing from help.js | `api_filaments_list`, `api_live_printers_partial`, `api_search` absent from HELP_SECTIONS. **Fix:** Added to appropriate section arrays. | 🟡 Medium | XS (Trivial) | Fixed in v1.111.0 |
| BUG-422 | `clear_history` missing explicit `@require_admin` decorator | `history.py:109-119`: No `@require_admin` decorator. **Fix:** Added `@require_admin`. | 🟡 Medium | XS (Trivial) | Fixed in v1.111.0 |
| BUG-511 | `validate_printer_host` has no hostname/IP validation | `utils/__init__.py:1697-1711`: Only checks if input starts with `http://` or `https://`. Does NOT validate hostname format, does NOT restrict dangerous addresses. **Fix:** Added hostname format validation (no spaces, valid chars), blocks localhost/loopback (127.x.x.x, ::1, 0.0.0.0, localhost). | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-512 | Third-party data leak via Jina Reader fallback in link previews | `utils/__init__.py:1588-1593`: When OpenGraph/Twitter metadata is insufficient, the app sends the URL to `r.jina.ai` (third-party service). Admins may not be aware their project link URLs are forwarded externally. **Fix:** Added `link_preview_reader_enabled` AppSetting (default: False); toggle in Settings → General tab. Reader fallback only runs when explicitly enabled. (models.py, migrations.py, settings.html, settings.py, utils/__init__.py, messages.py) | 🟡 Medium | S (Small) | Fixed in v1.114.2 |
| BUG-513 | DNS rebinding can bypass SSRF protection in `is_safe_external_url` | `utils/__init__.py:1299-1330`: DNS resolution happens at check time, but the actual HTTP request connects later. **Fix:** Added `_validate_peer_ip()` that inspects the actual connected socket IP after each HTTP request in `_follow_safe_redirects()` and `_fetch_reader_fallback()` — catches DNS rebinding by re-validating the peer address post-connect. | 🟡 Medium | M (Medium) | Fixed in v1.114.2 |
| BUG-514 | `utc_now()` returns naive datetime — Python 3.12+ compatibility risk | `utils/time_utils.py:34-41`: Returns naive datetime (no `tzinfo`). Python 3.12+ raises `TypeError` when comparing aware vs. naive datetimes. **Fix:** Added `UtcDateTime` TypeDecorator (normalises all reads to aware), changed `utc_now()` to return aware, added `utc_now_naive()` for explicit legacy use, updated `_SORT_EPOCH` to aware, fixed `_parse_ts()` in Bambu helpers. (models.py, time_utils.py, utils/__init__.py, routes/bambu_helpers.py) | 🟡 Medium | M (Medium) | Fixed in v1.115.0 |
| BUG-515 | ~30+ `db.session.commit()` calls across routes lack try/except | Multiple route files: 115+ `db.session.commit()` calls without try/except/rollback. **Fix:** Added `safe_commit()` utility wrapping commit in try/except with rollback and flash error message. Applied to all 10 user-facing route modules + 3 helper modules. (utils/__init__.py, messages.py, routes/*.py) | 🟡 Medium | L (Large) | Fixed in v1.115.0 |
| BUG-516 | `markdown-editor.js` uses deprecated `document.execCommand()` (7+ calls) | `static/js/markdown-editor.js:83-97`: `document.execCommand('bold')`, `'italic'`, etc. — deprecated API. **Fix:** Replaced with modern Selection/Range API using `_toggleInlineTag()`, `_wrapInline()`, `_execBlockTag()`, `_execList()` helpers. All formatting operations now use `createRange()` + `surroundContents()`. (static/js/markdown-editor.js) | 🟡 Medium | L (Large) | Fixed in v1.115.0 |
| BUG-517 | `inventory.js:152` — `alert()` for user-facing error (CSP-hostile) | `static/js/inventory.js:152`: `alert((window._filCtxT && window._filCtxT.addSpoolError) || 'Error adding spool.')` — `alert()` is blocking, non-stylable, and may be blocked by CSP. The app already has `window.showToast()`. **Fix:** Replace with `showToast()`. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-518 | `markdown-editor.js` uses `window.prompt()` for URL input | `static/js/markdown-editor.js:93,140`: `window.prompt('URL', 'https://')` — blocking, not stylable, may be blocked by popup blockers or CSP. **Fix:** Replaced with inline modal injected via `init()` — non-blocking Promise-based flow, supports dark mode, uses `window.__i18n` for labels. Added `md_url_prompt_title`/`cancel`/`ok` keys to `base.html`. | 🟡 Medium | S (Small) | Fixed in v1.114.2 |
| BUG-519 | Command palette search race condition — no AbortController | `static/js/app-shell.js:114-128`: `fetchResults()` fires on each keystroke via `$watch('query', ...)`. If a response for an earlier query arrives after a later one, stale results overwrite correct ones. **Fix:** Add AbortController to cancel previous in-flight requests. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-520 | `dashboard.js` — 8x `parseInt()` without radix parameter | `static/js/dashboard.js:912,914,1026,1028` (and 4 more): `parseInt(lim)`, `parseInt(row.dataset.rowIndex)` — missing the radix `, 10` parameter. Strings like `"08"` could be misinterpreted as octal in legacy contexts. **Fix:** Add `, 10` to all `parseInt()` calls. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-521 | `tour.js` — Hardcoded i18n strings bypass `window.__i18n` dictionary | `static/js/tour.js:750,754-755`: Tour UI uses inline ternaries `L === 'cs' ? 'Průvodce' : 'Tour'`, `'Zpět'/'Back'`, `'Dokončit'/'Finish'`, `'Dále'/'Next'` instead of the app's `window.__i18n` dictionary. **Fix:** Look up strings from `window.__i18n[lang]`. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-522 | `mobile-ux.js` — Hardcoded English fallback strings | `static/js/mobile-ux.js:80-82`: `'Pull down to refresh'`, `'Release to refresh'`, `'Loading...'` are hardcoded English fallbacks that bypass the i18n system. **Fix:** Use `window.__i18n` dictionary with fallback. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-523 | `models.py` — Multiple timestamp columns missing `nullable=False` | `models.py:245,315,341,622`: `PrintHistory.created_at`, `ProjectComment.created_at`, `ProjectFile.uploaded_at`, `WasteFile.uploaded_at` — all have `default=_utc_now` but no `nullable=False`. Inconsistent with other timestamp columns (e.g., `WasteRecord.created_at` at line 608 which has it). **Fix:** Add `nullable=False` to all. | 🟡 Medium | S (Small) | Fixed in v1.113.0 |
| BUG-543 | Login rate limiter uses spoofable `request.access_route[0]` (X-Forwarded-For) while registration uses `request.remote_addr` | `routes/auth.py:130`: Login rate limiter takes `client_ip = request.access_route[0] if request.access_route else request.remote_addr`. `request.access_route[0]` trusts the `X-Forwarded-For` header **unconditionally**. An attacker can bypass the rate limiter by setting different `X-Forwarded-For` headers. **Fix:** Changed to use `request.remote_addr` (consistent with registration endpoint). | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-544 | `model_delete_comment` doesn't verify comment belongs to the `root_id` model | `routes/models.py:668-675`: `model_delete_comment(root_id, comment_id)` fetches the comment by `comment_id` alone — does NOT verify that `comment.root_file_id == root_id`. Minor IDOR: user can delete their own comment via a different `root_id` URL. **Fix:** Added `if comment.root_file_id != root_id: abort(404)`. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-545 | `model_upload_thumbnail` accepts arbitrarily large base64 payload with no explicit size check | `routes/models.py:607-630`: `img_data = request.form.get('image')` is limited by `MAX_CONTENT_LENGTH` (64 MB global), but the entire base64 string is allocated in memory before `base64.b64decode()`. No app-level size limit. **Fix:** Added `if len(img_data) > 10 * 1024 * 1024: return jsonify(...), 413` before decoding. Added `error_image_too_large` i18n key. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-546 | `api_models_list` — `get_current_user().id` without None guard (potential `AttributeError`) | `routes/models.py:262-263`: `if not is_admin(): query = query.filter(Project.owner_user_id == get_current_user().id)` — if `get_current_user()` returns `None`, `.id` raises `AttributeError`. **Fix:** `user = get_current_user(); if not is_admin(): query = query.filter(Project.owner_user_id == (user.id if user else None))`. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-547 | `_fetch_reader_fallback` URL construction corrupts URLs with `http://` in query parameters | `utils/__init__.py:1589`: `reader_url = f"https://r.jina.ai/http://{_strip_fragment(url).replace('https://', '').replace('http://', '')}"` — if `url` contains `http://` in its query string, the `.replace()` chaining strips both the scheme prefix AND occurrences in query params. **Fix:** Replaced with `re.sub(r'^https?://', '', _strip_fragment(url))` — strips only the leading scheme. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-548 | Bulk user deactivation doesn't invalidate active sessions (inconsistency with single-user deactivation) | `routes/auth.py:426-434`: `_users_bulk_action()` deactivates users but never calls `invalidate_all_user_sessions()`. Single-user deactivation at line 338 does call it. **Fix:** Loop through deactivated user IDs and call `invalidate_all_user_sessions()` for each. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-549 | `_login_attempts` dict has unbounded growth — IPs are never purged | `routes/auth.py:38`: `_login_attempts = {}` grows indefinitely. Each new IP adds an entry never deleted. **Fix:** Now deletes IP entries when their pruned timestamp list becomes empty; hard reset at 1000+ entries to prevent unbounded growth. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |
| BUG-550 | `users.html:147` — `@submit.prevent` embeds translated string in Alpine JS `confirm()` call | `templates/users.html:147`: `@submit.prevent="if (bulkAction === 'delete') { if (!confirm('{{ t('users_bulk_delete_confirm')|e }}')) return; }"` — uses `|e` (HTML-escaping) but this is inside a JS string in an Alpine attribute. HTML-escaping is NOT JS-escaping. **Fix:** Replaced with `data-confirm-msg` attribute read via `$el.dataset.confirmMsg` in Alpine expression. | 🟡 Medium | S (Small) | Fixed in v1.114.1 |

---

## 🟢 Low Priority (Someday/Maybe)

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| BUG-558 | 43+ `app.logger.*()` calls use f-strings instead of lazy `%s` formatting — unnecessary string formatting even when log level is disabled | Across `routes/calculator.py`, `routes/backup.py` (4 instances), `routes/settings.py` (29+ instances), `migrations.py` (8 instances), `routes/history.py`: f-strings are used in logger calls like `app.logger.debug(f"...")`. Python logging best practice requires lazy `%s` formatting so the string is only interpolated when the log level is active. Most of these are `debug()` calls that would be skipped entirely. **Fix:** Convert to `app.logger.debug("Static template %s", variable)` pattern across all 43+ call sites. | 🟢 Low | M (Medium) | **Open** |
| BUG-559 | Inconsistent `app.logger` pattern: 5 route files use closure-captured `app` instead of `current_app.logger` | `routes/inventory.py:524`, `routes/history.py:117,120`, `routes/calculator.py:336`, `routes/settings.py:passim`, `routes/backup.py:passim` reference `app.logger` captured from the outer `register(app)` function closure, while `routes/projects.py:743` and `routes/auth.py:493` correctly use `current_app.logger`. The closure pattern works but is unconventional and fragile — moving the view function outside `register()` would break. **Fix:** Replace `app.logger` with `current_app.logger` consistently, importing `current_app` from flask. | 🟢 Low | M (Medium) | **Open** |
| BUG-579 | `models.py:659` — `WasteRecord.__repr__` triggers lazy-loaded relationship that raises `DetachedInstanceError` when session is closed | `models.py:659`: `return f'<WasteRecord {self.id} {self.filament.name if self.filament else "?"}...>'` — `self.filament` is a lazy-loaded relationship. If `__repr__` is called after the session is closed (error logs, debugging), accessing `self.filament` raises `DetachedInstanceError`. Other model `__repr__` methods already avoid this (e.g. `UserSession.__repr__` only uses scalar fields). **Fix:** Use `self.filament_id` (scalar FK) instead of `self.filament.name` (lazy relationship) in `__repr__`. | 🟢 Low | XS (Trivial) | **Open** |
| BUG-580 | Unused imports and dead code in `app.py`, `routes/calculator.py`, `tests/test_utils_extended.py`, `utils/markdown.py` | `app.py:35`: `from sqlalchemy import text` never used. `app.py:123,150`: Inner re-imports of `secrets` and `json` when both are available at module level. `routes/calculator.py:7`: `from datetime import datetime` — `datetime` never used directly. `tests/test_utils_extended.py:9`: `from database import db` never used. `utils/markdown.py:149`: Dead variable `list_type_local = None` with `# noqa: F841` suppressing the linter. **Fix:** Remove unused imports and the dead variable assignment. | 🟢 Low | S (Small) | **Open** |
| BUG-581 | Missing `x-cloak` on tab content `x-show` elements in `account.html` and lightbox modals in `project_detail.html` | `templates/account.html:101,119,189,231`: Four `x-show="activeTab === '...'"` elements on tab content containers lack `x-cloak`. Before Alpine initializes, all tab contents flash briefly. `templates/project_detail.html:895,915`: Lightbox and 3D viewer modal use `style="display: none;"` instead of `x-cloak`. **Fix:** Add `x-cloak` attribute to all `x-show` elements. | 🟢 Low | S (Small) | **Open** |
| BUG-582 | F-string logger calls in `routes/backup_helpers.py:580`, `routes/calculator.py:246,319,336`, `routes/backup.py:913` | These 5 additional f-string logger calls (not already covered by BUG-558) use eager formatting: `route/backup_helpers.py:580` (`app.logger.debug(f"Export: ...")`), `routes/calculator.py:246` (`app.logger.debug(f"Print calculated: ...")`), `routes/calculator.py:319` (`app.logger.info(f"Project quote saved: ...")`), `routes/calculator.py:336` (`app.logger.debug(f"Deleted print history: ...")`), `routes/backup.py:913` (`app.logger.warning(f"Skipping waste record...")`). **Fix:** Convert to lazy `%s` formatting. | 🟢 Low | S (Small) | **Open** |
| BUG-583 | Unbounded `.all()` queries in `routes/calculator.py` and `routes/inventory_helpers.py` — no pagination or LIMIT | `routes/calculator.py:199`: `Filament.query.all()`, line 200: `Project.query.order_by(...).all()`, line 313: `Brand.query.order_by(...).all()` — all load entire tables into memory. `routes/inventory_helpers.py:137`: `Filament.query.filter(Filament.quantity > 0).all()` — loads ALL stocked filaments for overview. Line 297: `Filament.query.options(...).all()` for action center. On large inventories (>5000) these cause memory pressure. **Fix:** Add LIMIT/pagination or push aggregation to DB level. | 🟢 Low | M (Medium) | **Open** |
| BUG-584 | README.md outdated: references `utils.py` instead of `utils/` package, "~24 tables" (~27+), missing `model_renderer.py` from route listing, malformed testing section with unclosed code block | `README.md:98`: Lists `utils.py` but codebase has `utils/` package (since v1.106.0). `README.md:95`: Says "~24 tables" but actual model count is 27+. `README.md:119-121`: Missing `routes/model_renderer.py`. `README.md:220-235`: Unclosed code block after testing section creates two overlapping code blocks. **Fix:** Update project structure, model count, add model_renderer.py, fix code block formatting. | 🟢 Low | S (Small) | **Open** |
| BUG-585 | Weak/vacuous test assertions across `test_bambu_extended.py`, `test_projects_extended.py`, `test_inventory_extended.py`, `test_prusa_extended.py`, `test_waste_extended.py` | Multiple tests assert only `status_code == 200` without verifying content: `test_bambu_extended:101-108` (filament filter, pagination — no content check), `test_prusa_extended:99-114` (4 filter tests — no content check), `test_waste_extended:178-192` (3 filter/pagination tests — no content check), `test_inventory_extended:742-752` (onboarding dismissed — no content check), `test_bambu_extended:278` (auto_map_history accepts both 200 and 302 — too broad). **Fix:** Add meaningful content assertions (check for expected elements or records in HTML/JSON responses). | 🟢 Low | M (Medium) | **Open** |
| BUG-586 | Minor JS issues: `parseInt()` without radix in dashboard.js, `confirm()` in inventory.js, `for...in` in app-shell.js fetch override | `static/js/dashboard.js:1050,1092,1093`: `parseInt(value)` and `parseInt(savedSize)` lack radix `, 10` parameter — strings starting with `0x` could be misinterpreted as hex. `static/js/inventory.js:257`: `confirm(t.confirmDelete || 'Delete?')` — blocking, non-stylable dialog (CSP-safe but poor UX). `static/js/app-shell.js:221-232`: `for (var k in opts)` — enumerates prototype properties; should use `Object.keys(opts)`. **Fix:** Add radix `, 10` to all `parseInt()` calls, replace confirm with modal, use `Object.keys()`. | 🟢 Low | S (Small) | **Open** |
| BUG-587 | `encode_backup_payload` helper function copy-pasted identically in `test_backup_extended.py:21-36` and `test_settings.py:51-65` | Both files define the exact same `encode_backup_payload` helper with identical logic. Violates DRY — if the helper needs updating, both copies must be changed in sync. **Fix:** Extract to a shared `tests/helpers.py` module. | 🟢 Low | S (Small) | **Open** |
| BUG-588 | Dead code in `tests/test_waste_extended.py:53-59` — `WasteRecord` created with hardcoded IDs, never persisted | Lines 53-59 create a `WasteRecord(filament_id=1, ...)` before the admin/filament are flushed to the DB. This object is NEVER added to the session. Lines 63-72 create a new correct `WasteRecord` and persist it. The first assignment is dead code. **Fix:** Remove the dead `WasteRecord` creation on lines 53-59. | 🟢 Low | XS (Trivial) | **Open** |
| BUG-589 | `tests/test_ui_improvements.py:96-98` — test posts reaction to non-existent comment (ID 0) with no assertion | Posts to `comments/0/react` with `follow_redirects=True`. Since no comment with ID 0 exists, the POST presumably fails silently. The test only checks the subsequent GET response for HTML strings, never verifying the reaction worked or failed. **Fix:** Create a real comment first and use its actual ID, or remove the reaction POST if not needed for the assertions that follow. | 🟢 Low | S (Small) | **Open** |
| BUG-200 | XSS vector in markdown editor link rendering | `markdown-editor.js:334`: URL interpolated directly into `href`. **Fix:** Added attribute escaping and restricted URL regex. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-201 | XSS vector in inventory context menu (data-attribute breakout) | `inventory.js:205`: URLs from `el.dataset` interpolated without escaping. **Fix:** Added `attrEsc()` helper. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-202 | `saveLayout()` called on every `dragenter` causing excessive writes | `dashboard.js:698`: `onDragEnter` fires on every `dragenter`. **Fix:** Debounced via `requestAnimationFrame`. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-204 | Weak chart data assertion | `test_stats_extended.py:196-202`: `self.assertIn('labels', html)`. **Fix:** Checks for `const statsData` and JSON. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-205 | Vacuously true assertion in test_utils_extended | `test_utils_extended.py:402-408`: `self.assertNotEqual(result, 'https://')`. **Fix:** Now checks `isinstance(str)` and `'://' in result`. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-206 | Loose backup count assertion | `test_backup_extended.py:302-304`: `assertLessEqual(len(remaining), 4)`. **Fix:** Now asserts exactly 3. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-207 | `file_size_bytes` uses `db.Integer` — overflow for files >2GB | `models.py:345`: `Integer`. **Fix:** Changed to `db.BigInteger`. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-208 | Missing `x-cloak` on `x-show` element in `_projects_layout.html` | `_projects_layout.html:154`: `<a ... x-show="hasFilters">`. **Fix:** Added `x-cloak`. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-209 | MutationObserver setup may miss `DOMContentLoaded` | `app-shell.js:174`: Edge case. **Fix:** Check `document.readyState` before adding listener. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-210 | Redundant conditional branches in dashboard.js drag-and-drop | `dashboard.js:661-678`: Identical `if`/`else if` branches. **Fix:** Merged conditions. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-211 | Three models not covered by backup/export | `UserSession`, `ModelComment`, `ProjectCommentReaction`. **Fix:** Added to export/import. | 🟢 Low | S (Small) | Fixed in v1.109.1 |
| BUG-212 | Waste test missing response check | `test_waste.py:106-114`: POSTs data but never checks response. **Fix:** Added status assertion. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-213 | Storage test missing response check | `test_storage_history_pwa.py:88-97`: Posts duplicate shelf name without checking response. **Fix:** Added status assertion. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-214 | UI mode test doesn't verify session change | `test_inventory_extended.py:493-502`: Checks only status code. **Fix:** Added session check. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-215 | Hardcoded English error messages in bambu.py JSON API | `routes/bambu.py:285,628`. **Fix:** Replaced with `translate()` calls. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-216 | f-string in auto-backup log instead of lazy `%s` | `app.py:659-662`. **Fix:** Changed to lazy `%s` formatting. | 🟢 Low | S (Small) | Fixed in v1.110.0 |
| BUG-423 | Monetary fields use `db.Float` instead of `db.Numeric` | `models.py:131,163,244,255-260`: Subject to IEEE 754 precision errors for currency. Should use `db.Numeric(10,2)`. | 🟢 Low | M (Medium) | Fixed in v1.116.0 |
| BUG-424 | Keydown event listener never cleaned up in app-shell.js | `static/js/app-shell.js:66-93`: Listener captures stale `this` if Alpine component is replaced via AJAX. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-425 | Context menu event listeners never cleaned up | `static/js/inventory.js:262-271`: Listeners persist with stale references after AJAX replacement. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-426 | Dashboard drag/resize listener cleanup issues | `static/js/dashboard.js:503-520,746-757`: Module-level variables could hold stale references after multiple init calls. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-427 | `javascript:` URLs accepted in markdown editor | `static/js/markdown-editor.js:93,139-146`: Visual and source editors accept any URL scheme. **Fix:** Added `https://` URL scheme validation. | 🟢 Low | S (Small) | Fixed in v1.111.0 |
| BUG-428 | Redundant `|safe` after `|tojson` in templates | `bambu.html:113`: `{{ _bs_init_data|tojson|safe }}`. **Fix:** Removed redundant `|safe`. | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-429 | Deprecated `document.execCommand('copy')` | `models_detail.html:52`. **Fix:** Replaced with `navigator.clipboard.writeText()`. | 🟢 Low | S (Small) | Fixed in v1.111.0 |
| BUG-430 | 7 migration-created indexes missing from model `index=True` | 7 columns have indexes in migrations but not `index=True` on model. **Fix:** Added `index=True` to all 7 column definitions. | 🟢 Low | S (Small) | Fixed in v1.111.0 |
| BUG-431 | Redundant index creation for AuditLog columns | `migrations.py:74-76`: Redundant CREATE INDEX for columns with `index=True`. **Fix:** Added explanatory comment (kept for legacy compat). | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-432 | `x-show` without `x-cloak` — FOUC on 12 elements | `add.html:33,55,86`, `_projects_layout.html:192,210`, `bambu.html:144,179`, `storage.html:313`, `index.html:226,256,287,315`. **Fix:** Added `x-cloak` to all. | 🟢 Low | S (Small) | Fixed in v1.111.0 |
| BUG-433 | Dead code in auth.py `pwa.` prefix check | `auth.py:365`: `endpoint.startswith('pwa.')` is dead code. **Fix:** Removed the check. | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-434 | Redundant imports in inventory.py | `import csv` and `import io` at module level AND inside functions. **Fix:** Removed module-level imports (kept inside functions). | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-435 | `file_size_bytes` type mismatch: model=BigInteger, migration=INTEGER | `migrations.py:190,324` used `INTEGER`. **Fix:** Changed to `BIGINT`. | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-436 | Hardcoded client-side i18n strings in base.html | `base.html:383-404`: Bypasses `t()` system. **Fix:** Restructured with `|tojson` and sync comment. | 🟢 Low | S (Small) | Fixed in v1.111.0 |
| BUG-437 | `waste.html:150` — redundant `|forceescape` after `|tojson` | Double-escalates. **Fix:** Removed redundant `|forceescape». | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-438 | Hardcoded English in overview.html action type fallback | `overview.html:362`: `{{ entry.action_type | replace('_',' ') | title }}`. **Fix:** Now uses `t()` with fallback. | 🟢 Low | XS (Trivial) | Fixed in v1.111.0 |
| BUG-524 | Stale `overview_user` entry in `SECTION_BY_ENDPOINT` (dead code) | `auth.py:209`: `'overview_user': SECTION_OVERVIEW` — no Flask route function named `overview_user` exists. The `index()` function renders `overview_user.html` based on role, but the endpoint name is `inventory.index`. Dead mapping that could mislead developers. **Fix:** Remove the stale entry. | 🟢 Low | XS (Trivial) | Fixed in v1.113.0 |
| BUG-525 | `StoragePlacement` lacks unique constraint on `(shelf_id, slot_index)` | `models.py:457-465`: Two filaments could theoretically be placed in the same slot of the same shelf. Application code checks for duplicates (`routes/storage.py:218`), but there's no DB-level enforcement via `__table_args__` `UniqueConstraint`. **Fix:** Add `UniqueConstraint('shelf_id', 'slot_index', name='uq_shelf_slot')`. | 🟢 Low | S (Small) | Fixed in v1.115.1 |
| BUG-526 | Missing `aria-label` on 20+ icon-only buttons across templates | `_filament_cards.html:107-115`, `_filament_list_rows.html:70-78,634-651`, `project_detail.html:636-651`, `storage.html:442-443,536`, `_project_files.html:38-45`, `base.html:249,361`: Icon-only buttons have `title=` but no `aria-label`. Screen readers cannot identify these buttons. **Fix:** Add `aria-label="{{ t('key') }}"` to all icon-only buttons. | 🟢 Low | M (Medium) | Fixed in v1.116.0 |
| BUG-527 | Missing `x-cloak` on flash toast and undo toast | `base.html:524-546`: Flash toast uses `x-show="show"` without `x-cloak` — brief flash on page load. `_toast_undo.html:10`: Same pattern. `audit.html:96`: `x-show="open"` on expandable row without `x-cloak`. **Fix:** Add `x-cloak` to all. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-528 | `PrusaPrintJob.project_id` lacks index | `models.py:554`: `project_id` FK has no `index=True`. For comparison, `BambuPrintJob.project_id` (line 495) has `index=True`. Prusa print jobs are queried by project association, causing full table scans. **Fix:** Add `index=True`. | 🟢 Low | XS (Trivial) | Fixed in v1.113.0 |
| BUG-529 | `MovementHistory.bambu_job_id` lacks index | `models.py:159`: `bambu_job_id` FK has no `index=True`. Movement history entries are queried by bambu job ID for undo operations and audit trail. **Fix:** Add `index=True`. | 🟢 Low | XS (Trivial) | Fixed in v1.113.0 |
| BUG-530 | `AppSetting` model/migration nullable mismatch (~24 columns) | `models.py:177-237`: Columns like `kwh_price`, `currency`, `debug_logging`, `theme`, `view_mode`, `items_per_page`, `bambu_region`, etc. are defined without `nullable=False` in the model but migrations use `NOT NULL`. Fresh databases (via `db.create_all()`) get NULLABLE columns; legacy databases (via migration) get NOT NULL. **Fix:** Add `nullable=False` to all corresponding model columns. | 🟢 Low | M (Medium) | Fixed in v1.115.1 |
| BUG-531 | `_migrate_waste_record_fk` drops indexes when recreating tables | `migrations.py:351-418`: Recreates `waste_record` and `waste_file` tables but the new definitions lack indexes: `waste_file.waste_record_id` (model says `index=True`) and composite index `ix_waste_record_filament_project`. Indexes are missing until next restart. **Fix:** Add `CREATE INDEX` statements inside the migration after recreating tables. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-532 | `history.html:30` — translated string in JS `onclick` context | `history.html:30`: `onclick="return confirm('{{ t('clear_history_confirm') }}')"` — translates string is embedded unescaped into JS string context. If translation contains `'` or `</script>`, it breaks JS or injects code. **Fix:** Use `data-confirm` attribute pattern (already exists in `base.html`). | 🟢 Low | S (Small) | Fixed in v1.113.0 |
| BUG-533 | `models_detail.html` — 5 hardcoded English strings | `models_detail.html:130,153,199,882,884,888`: `'Save Thumbnail'`, `'Adjust the preview material tint...'`, `'No description provided for this version.'`, `'Models thumbnail successfully captured!'`, `'Failed to save captured thumbnail.'`, `'Error sending thumbnail to server.'` — all hardcoded English. **Fix:** Replace with `{{ t("key") }}`. | 🟢 Low | S (Small) | Fixed in v1.113.0 |
| BUG-534 | `api.py:203` — Hardcoded Czech fallback in search results | `routes/api.py:203`: `'subtitle': p.client_name or '... klienta neuveden'` — hardcoded Czech. **Fix:** Use `translate('client_unnamed')`. | 🟢 Low | XS (Trivial) | Fixed in v1.113.0 |
| BUG-535 | Hardcoded English/Czech strings in `log_movement()` notes (15+ instances) | `routes/inventory.py:258,308,337,339,357,400,427`, `routes/projects.py:1024`, `routes/bambu.py:404,409,450,461,517,579,590`, `routes/prusa.py:337`: Notes like `'Bulk delete'`, `'Initial stock'`, `'Bambu job: ...'`, `'PrusaLink: ...'` are hardcoded. These appear in the movement history UI. **Fix:** Use `translate()` keys. | 🟢 Low | M (Medium) | Fixed in v1.115.1 |
| BUG-536 | `waste.py:95` — All waste records loaded to compute total (performance) | `routes/waste.py:95`: `total_waste = sum(r.weight_grams for r in WasteRecord.query.all())` — loads ALL waste records into memory just to compute a SUM. **Fix:** Use `db.session.query(db.func.sum(WasteRecord.weight_grams)).scalar() or 0`. | 🟢 Low | S (Small) | Fixed in v1.113.0 |
| BUG-537 | `models.py:287` — Unbounded API query loads all models into memory | `routes/models.py:287`: `models_list = query.all()` — no pagination at DB level. Pagination is done in-memory on the `enriched` list (lines 320-330). For large model collections, this loads all results. **Fix:** Apply pagination at the DB query level. | 🟢 Low | S (Small) | Fixed in v1.115.1 |
| BUG-538 | 4 `TODO(security)` comments indicate incomplete origin validation | `templates/project_detail.html:1154,1160`, `static/viewer.html:69,249`: Security TODO comments about cross-origin iframe communication validation. These indicate known incomplete origin validation for postMessage handling. **Fix:** Review and complete origin validation logic. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-551 | `_login_attempts` dict not protected by a lock — multi-thread race condition | `routes/auth.py:38-60`: `_login_attempts = {}` is accessed by 4 Gunicorn threads without synchronization. The check-prune-append sequence spans multiple steps not atomically protected. **Fix:** Wrapped function body in `threading.Lock()` to prevent logical race conditions on rate limit checks. | 🟢 Low | S (Small) | Fixed in v1.114.1 |
| BUG-552 | `models_index.py` stats: loads all model file IDs into memory for total size computation | `routes/models.py:271-276`: `ProjectFile.id.in_([f.id for f in base_q.with_entities(ProjectFile.id).all()])` fetches all model IDs into Python, then issues a second query with a large `IN (...)` clause. For databases with thousands of models, this loads a large list into memory unnecessarily. **Fix:** Use a subquery: `db.session.query(db.func.sum(ProjectFile.file_size_bytes)).filter(base_q.subquery().c.id == ProjectFile.id).scalar()`. | 🟢 Low | S (Small) | Fixed in v1.116.0 |
| BUG-553 | `project_create` and `project_edit` accept past due dates without warning | `routes/projects.py:274,443`: `due_date = datetime.strptime(due_date_str, '%Y-%m-%d') if due_date_str else None` — no validation that the due date is not in the past. Projects can be created/edited with a due date 20 years ago, immediately appearing as "overdue" in the Action Center. This is a UX issue (no warning) rather than a security issue. **Fix:** Optionally validate that `due_date >= utc_now().date()` and flash a warning (not error, since backdating may be intentional). | 🟢 Low | XS (Trivial) | Fixed in v1.116.0 |

---

## 🧹 Technical Debt

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| TD-001 | Unify `utils/` package refactor | `utils/` was split from `utils.py` in v1.106.0. Some code may still reference old `utils.` import paths internally. Audit and clean up. | 🟡 Medium | S (Small) | Backlog |
| TD-002 | Remove deprecated `datetime.utcnow()` patterns | Python 3.12+ deprecation. All code already uses `utc_now()`, but verify zero callers remain. | 🟢 Low | S (Small) | Ongoing |
| TD-003 | Alembic migration support | Replace `_safe_alter()` manual migrations with proper Alembic-based migration tracking for production safety. | 🟡 Medium | M (Medium) | Backlog |
| TD-004 | DRY: 30+ test classes duplicate identical `create_app({...})` boilerplate | Every test class has the same 5 config keys repeated. No `conftest.py` or shared base class provides this. | 🟢 Low | M (Medium) | Backlog |
| TD-005 | Wall-clock timing assertions in benchmarks are inherently flaky | `test_performance.py:90-134`: Time-based `assertLess(elapsed, 2000)` can produce intermittent failures in CI, constrained environments, or under load. | 🟢 Low | S (Small) | Backlog |
| TD-006 | `test_waste.py:69-72` — no `AUTH_REQUIRED_IN_TESTS` config, depends on app default | Unlike other test suites, `test_waste.py` doesn't explicitly set auth config. If default changes, test becomes vacuous. | 🟢 Low | S (Small) | Backlog |

---

## 📋 Feature Requests

| ID | Title | Description | Criticality | Effort | Status |
|----|-------|-------------|-------------|--------|--------|
| BL-001 | OctoPrint Integration | Add OctoPrint printer integration alongside Bambu/Prusa | 🟡 Medium | L (Large) | Backlog |
| BL-002 | REST API | Public REST API for third-party integrations | 🟡 Medium | XL (Extra Large) | Backlog |
| BL-003 | RFID/NFC Spool Pairing | Tag filament spools with RFID/NFC for automatic identification | 🟡 Medium | L (Large) | Backlog |
| BL-012 | Bambu: Manual Print Duplicate | Ruční duplikace tisku z Bambu tiskárny. **Fix:** Implementována POST route `/bambu/job/<id>/duplicate` pro ruční duplikování tisku. Nová kopie má `external_id` s prefixem `manual-dup-` a `deducted=False` pro nezávislé přiřazení filamentu. | 🟡 Medium | S (Small) | Fixed in v1.119.6 |
| BL-013 | Project tags on model views | Modely nahrané přes projekty nyní přejímají a zobrazují štítky svého projektu. Projektové štítky se zobrazují na kartách modelů, v řádcích tabulky i na stránce detailu modelu. | 🟢 Low | XS (X-Small) | Fixed in v1.119.7 |
| BL-015 | Bambu waste modal: interactive filament search + AMS auto-suggest | Interaktivní filamentový vyhledávač v modálním okně zmetku na stránce Bambu. Automatické předvyplnění filamentu dle barvy a materiálu z AMS. | 🟢 Low | S (Small) | Fixed in v1.119.10 |
| BL-006 | PostgreSQL Support (4.2) | **PostgreSQL podpora jako alternativa k SQLite.** Dialektová abstrakce v `database.py` (`detect_dialect()`, `engine_options_for()`), `DATABASE_URL` env var pro výběr databáze, automatické nastavení connection poolu pro PostgreSQL (`pool_size=10`, `max_overflow=20`, `pool_pre_ping=True`), `psycopg2-binary` driver. Docker Compose rozšířen o `postgres:16-alpine` service s healthcheckem a persistentním volume. Migrační script `migrate_to_pg.py` pro přechod z SQLite na PostgreSQL. Migrační guide v README. `_safe_alter()` a migrace ošetřeny pro PostgreSQL rezervovaná slova (`"user"`). SQLite-only operace (`PRAGMA`, table recreation) hlídané dialect checkem. | 🟡 Medium | L (Large) | Fixed in v1.118.0 |

| BL-005 | Interactive Notification Bell with Live Counter | **Notifikační zvonek s live counterem v topbaru.** Interaktivní dropdown s Alpine.js zobrazující poslední notifikace s ikonami podle typu, relativním časem, pulzující animací nových notifikací. AJAX polling každých 30s aktualizuje počet nepřečtených. Nové API endpointy: `/api/notifications/unread-count`, `/api/notifications/recent`, `/api/notifications/<id>/mark-read`, `/api/notifications/mark-all-read`. **Obsahuje:** 4 nové endpointy v `routes/auth.py`, Alpine.js komponenta v `base.html`, `notificationBell()` funkce v `app-shell.js`, CSS animace v `app.css`, i18n klíč `notifications_see_all` (cs+en), `SECTION_BY_ENDPOINT` a `help.js` aktualizace. | 🟡 Medium | M (Medium) | Fixed in v1.117.0 |
| BL-004 | Model Categories (3D model classification) | **Stránka modelů — kategorizace 3D modelů.** Každý model lze zařadit do uživatelsky definované kategorie (např. *Kuchyňské doplňky*, *Cosplay*, *Technické díly* apod.). Kategorie si uživatel sám přidává/spravuje. Výchozí kategorie pro nové modely je *Nezařazeno*. Filtr na stránce modelů rozšířen o interaktivní vyhledávací dropdown pro kategorii — stejné UX jako existující fulltext filtry (značka, materiál, barva). **Obsahuje:** nový model `ModelCategory` (id, name, color, created_at), FK `category_id` na `ProjectFile`, migrace `_safe_alter()`, CRUD pro kategorie v nastavení, Alpine.js fulltext dropdown filtr, i18n klíče cs+en, export/import pokrytí, help.js aktualizace. | 🟡 Medium | L (Large) | Fixed in v1.114.0 |

---

## 📊 Completed (moved from backlog)

| ID | Title | Completed In | Date |
|----|-------|-------------|------|
| BUG-001 | `_migrate_nullable_project_id` CREATE TABLE missing columns | v1.108.2 | 2026-06-07 |
| BUG-002 | Help panel `currentSection` detection broken (blueprint prefix) | v1.108.2 | 2026-06-07 |
| BUG-003 | Missing `PRAGMA foreign_keys=ON` | v1.108.2 | 2026-06-07 |
| BUG-004 | `WasteRecord.filament_id` NOT NULL FK without `ondelete` | v1.108.2 | 2026-06-07 |
| BUG-006 | Test `test_job_delete_nonexistent_returns_404` wrong assertion | v1.108.2 | 2026-06-07 |
| BUG-301 | `utils.translate()` ignores per-user `preferred_language` | v1.109.0 | 2026-06-07 |
| BUG-101 | Filament FK columns no `ondelete` | v1.109.0 | 2026-06-07 |
| BUG-005 | CSP nonce missing from template-specific `<script>` blocks | v1.109.0 | 2026-06-07 |
| BUG-102 | Hardcoded strings in route error responses | v1.109.0 | 2026-06-07 |
| BUG-103 | Hardcoded text in templates | v1.109.0 | 2026-06-07 |
| BUG-302 | `encrypt_token` silently returns plaintext on invalid Fernet key | v1.109.1 | 2026-06-07 |
| BUG-303 | `log_movement()` docstring warns about commit responsibility | v1.109.1 | 2026-06-07 |
| BUG-100 | Missing `ondelete` on MovementHistory + BambuJobMaterial FK columns | v1.109.1 | 2026-06-07 |
| BUG-306 | 3 models missing from backup export/import | v1.109.1 | 2026-06-07 |
| BUG-007 | Test `test_advance_status_full_flow` zero assertions | v1.110.0 | 2026-06-07 |
| BUG-008 | Test `test__project_usage_rows` impossible-to-fail assertion | v1.110.0 | 2026-06-07 |
| BUG-009 | Test `test_delete_filament_cleans_project_references` empty | v1.110.0 | 2026-06-07 |
| BUG-010 | Tour engine hardcoded Czech tooltip | v1.110.0 | 2026-06-07 |
| BUG-011 | `showToast` i18n dead code | v1.110.0 | 2026-06-07 |
| BUG-012 | Hardcoded `Rows`/`Color`/`Resize` in dashboard.js | v1.110.0 | 2026-06-07 |
| BUG-013 | Duplicate `openReorderShop` in inventory.js | v1.110.0 | 2026-06-07 |
| BUG-104 | Missing `bambu_auto_map_history` in help.js | v1.110.0 | 2026-06-07 |
| BUG-105 | Missing `project_undo` in help.js | v1.110.0 | 2026-06-07 |
| BUG-106 | ProjectFile FK/ORM cascade mismatch | v1.110.0 | 2026-06-07 |
| BUG-107 | Missing `db.session.rollback()` in POST handlers | v1.110.0 | 2026-06-07 |
| BUG-108 | No Prusa HTTP endpoint tests | v1.110.0 | 2026-06-07 |
| BUG-109 | Duplicate test in test_stats_extended | v1.110.0 | 2026-06-07 |
| BUG-110 | Dead code in test_bambu_extended._login_admin | v1.110.0 | 2026-06-07 |
| BUG-111 | loadScript caches rejected promises | v1.110.0 | 2026-06-07 |
| BUG-112 | model_public_share no test coverage | v1.110.0 | 2026-06-07 |
| BUG-200 | XSS vector in markdown editor | v1.110.0 | 2026-06-07 |
| BUG-201 | XSS vector in inventory context menu | v1.110.0 | 2026-06-07 |
| BUG-202 | saveLayout on every dragenter | v1.110.0 | 2026-06-07 |
| BUG-204 | Weak chart data assertion | v1.110.0 | 2026-06-07 |
| BUG-205 | Vacuously true assertion in test_utils_extended | v1.110.0 | 2026-06-07 |
| BUG-206 | Loose backup count assertion | v1.110.0 | 2026-06-07 |
| BUG-207 | file_size_bytes Integer overflow | v1.110.0 | 2026-06-07 |
| BUG-208 | Missing x-cloak in _projects_layout.html | v1.110.0 | 2026-06-07 |
| BUG-209 | MutationObserver DOMContentLoaded edge case | v1.110.0 | 2026-06-07 |
| BUG-210 | Redundant drag-and-drop branches | v1.110.0 | 2026-06-07 |
| BUG-212 | Waste test missing response check | v1.110.0 | 2026-06-07 |
| BUG-213 | Storage test missing response check | v1.110.0 | 2026-06-07 |
| BUG-214 | UI mode test doesn't verify session | v1.110.0 | 2026-06-07 |
| BUG-215 | Hardcoded English in bambu.py JSON API | v1.110.0 | 2026-06-07 |
| BUG-216 | f-string in auto-backup log | v1.110.0 | 2026-06-07 |
| BUG-305 | maintenance.py printer_id validation | v1.110.0 | 2026-06-07 |
| BUG-307 | Hardcoded placeholder text | v1.110.0 | 2026-06-07 |
| BUG-308 | Audit drops failed-write entries | v1.110.0 | 2026-06-07 |
| BUG-400 | Migration column order swap (silent data corruption) | v1.111.0 | 2026-06-07 |
| BUG-401 | PRAGMA foreign_keys=OFF leak on failure | v1.111.0 | 2026-06-07 |
| BUG-402 | Project FK missing ondelete | v1.111.0 | 2026-06-07 |
| BUG-403 | ProjectFile FK mismatch (CASCADE vs SET NULL) | v1.111.0 | 2026-06-07 |
| BUG-404 | Systemic onsubmit JS injection (44 instances) | v1.111.0 | 2026-06-07 |
| BUG-405 | No rate limiting on login | v1.111.0 | 2026-06-07 |
| BUG-406 | No password policy enforcement | v1.111.0 | 2026-06-07 |
| BUG-407 | Missing CSP nonce on base.html script tags | v1.111.0 | 2026-06-07 |
| BUG-408 | Hardcoded English strings in JSON API errors | v1.111.0 | 2026-06-07 |
| BUG-409 | Hardcoded URL paths instead of url_for() | v1.111.0 | 2026-06-07 |
| BUG-410 | Unsafe |escape in JS string context | v1.111.0 | 2026-06-07 |
| BUG-411 | project_query.whereclause internal SQLAlchemy API | v1.111.0 | 2026-06-07 |
| BUG-412 | calculator.py unprotected db.session.commit() | v1.111.0 | 2026-06-07 |
| BUG-413 | Fernet encryption silently falls back to plaintext | v1.111.0 | 2026-06-07 |
| BUG-414 | Session invalidation on password change | v1.111.0 | 2026-06-07 |
| BUG-415 | messages.py 2 keys missing from en dict | v1.111.0 | 2026-06-07 |
| BUG-416 | messages.py 4 translation errors | v1.111.0 | 2026-06-07 |
| BUG-417 | Missing CSP nonce on standalone pages | v1.111.0 | 2026-06-07 |
| BUG-418 | Raw comment.body_html (XSS via markdown) | v1.111.0 | 2026-06-07 |
| BUG-419 | JS string injection via translated strings in x-text | v1.111.0 | 2026-06-07 |
| BUG-420 | overview_user missing from SECTION_BY_ENDPOINT | v1.111.0 | 2026-06-07 |
| BUG-421 | 3 AJAX endpoints missing from help.js | v1.111.0 | 2026-06-07 |
| BUG-422 | clear_history missing @require_admin decorator | v1.111.0 | 2026-06-07 |
| BUG-427 | javascript: URLs accepted in markdown editor | v1.111.0 | 2026-06-07 |
| BUG-428 | Redundant |safe after |tojson | v1.111.0 | 2026-06-07 |
| BUG-429 | Deprecated document.execCommand('copy') | v1.111.0 | 2026-06-07 |
| BUG-430 | 7 migration-created indexes missing from model | v1.111.0 | 2026-06-07 |
| BUG-431 | Redundant index creation for AuditLog columns | v1.111.0 | 2026-06-07 |
| BUG-432 | x-show without x-cloak (FOUC on 12 elements) | v1.111.0 | 2026-06-07 |
| BUG-433 | Dead code in auth.py pwa. prefix check | v1.111.0 | 2026-06-07 |
| BUG-434 | Redundant imports in inventory.py | v1.111.0 | 2026-06-07 |
| BUG-435 | file_size_bytes type mismatch | v1.111.0 | 2026-06-07 |
| BUG-436 | Hardcoded client-side i18n strings in base.html | v1.111.0 | 2026-06-07 |
| BUG-437 | Redundant |forceescape after |tojson | v1.111.0 | 2026-06-07 |
| BUG-438 | Hardcoded English in overview.html action type fallback | v1.111.0 | 2026-06-07 |
| BUG-500 | UserSession export `last_activity` → `last_activity_at` | v1.113.0 | 2026-06-07 |
| BUG-501 | UserSession export non-existent `expires_at` column | v1.113.0 | 2026-06-07 |
| BUG-502 | XSS via `chart_data|safe` → `chart_data|tojson` | v1.113.0 | 2026-06-07 |
| BUG-503 | RBAC bypass on unmapped endpoints (default deny) | v1.113.0 | 2026-06-07 |
| BUG-504 | 15+ hardcoded Czech strings in calculator_project.html | v1.113.0 | 2026-06-07 |
| BUG-505 | Zero-price/weight filament creation logic bug | v1.113.0 | 2026-06-07 |
| BUG-506 | BambuPrintJob.deducted missing nullable=False | v1.113.0 | 2026-06-07 |
| BUG-507 | ProjectFilament.is_used missing nullable=False | v1.113.0 | 2026-06-07 |
| BUG-508 | ICS injection in maintenance calendar export | v1.113.0 | 2026-06-07 |
| BUG-509 | No rate limiting on /register endpoint | v1.113.0 | 2026-06-07 |
| BUG-510 | No session invalidation on role/permission change | v1.113.0 | 2026-06-07 |
| BUG-517 | alert() → showToast() in inventory.js | v1.113.0 | 2026-06-07 |
| BUG-519 | Command palette search race condition (AbortController) | v1.113.0 | 2026-06-07 |
| BUG-520 | parseInt() without radix in dashboard.js | v1.113.0 | 2026-06-07 |
| BUG-521 | Hardcoded i18n in tour.js | v1.113.0 | 2026-06-07 |
| BUG-522 | Hardcoded English in mobile-ux.js | v1.113.0 | 2026-06-07 |
| BUG-523 | Timestamp columns missing nullable=False | v1.113.0 | 2026-06-07 |
| BUG-524 | Stale overview_user in SECTION_BY_ENDPOINT | v1.113.0 | 2026-06-07 |
| BUG-528 | PrusaPrintJob.project_id missing index | v1.113.0 | 2026-06-07 |
| BUG-529 | MovementHistory.bambu_job_id missing index | v1.113.0 | 2026-06-07 |
| BUG-532 | history.html JS string injection (data-confirm) | v1.113.0 | 2026-06-07 |
| BUG-533 | 5 hardcoded English strings in models_detail.html | v1.113.0 | 2026-06-07 |
| BUG-534 | Hardcoded Czech in api.py search results | v1.113.0 | 2026-06-07 |
| BUG-536 | waste.py performance (DB aggregate) | v1.113.0 | 2026-06-07 |
| BL-004 | Model Categories (3D model classification) | v1.114.0 | 2026-06-07 |
| BUG-539 | confirm() JS injection in settings.html | v1.114.1 | 2026-06-08 |
| BUG-540 | confirm() in Alpine @submit.prevent (models_index.html) | v1.114.1 | 2026-06-08 |
| BUG-541 | abort(401) → abort(403) in 6 file ownership checks | v1.114.1 | 2026-06-08 |
| BUG-542 | Empty project name validation | v1.114.1 | 2026-06-08 |
| BUG-511 | validate_printer_host hostname validation | v1.114.1 | 2026-06-08 |
| BUG-543 | Login rate limiter X-Forwarded-For spoofing fix | v1.114.1 | 2026-06-08 |
| BUG-544 | model_delete_comment root_id ownership check | v1.114.1 | 2026-06-08 |
| BUG-545 | model_upload_thumbnail size limit before decode | v1.114.1 | 2026-06-08 |
| BUG-546 | api_models_list None guard for get_current_user() | v1.114.1 | 2026-06-08 |
| BUG-547 | _fetch_reader_fallback URL construction fix | v1.114.1 | 2026-06-08 |
| BUG-548 | Bulk user deactivation session invalidation | v1.114.1 | 2026-06-08 |
| BUG-549 | _login_attempts dict periodic cleanup | v1.114.1 | 2026-06-08 |
| BUG-550 | users.html |e → data-confirm-msg pattern | v1.114.1 | 2026-06-08 |
| BUG-551 | _login_attempts thread lock | v1.114.1 | 2026-06-08 |
| BUG-512 | Jina Reader link preview opt-in setting | v1.114.2 | 2026-06-08 |
| BUG-513 | DNS rebinding peer IP validation | v1.114.2 | 2026-06-08 |
| BUG-518 | Replace window.prompt() with inline modal | v1.114.2 | 2026-06-08 |
| BUG-514 | UtcDateTime TypeDecorator + utc_now() aware | v1.115.0 | 2026-06-08 |
| BUG-515 | safe_commit() try/except on all user-facing commits | v1.115.0 | 2026-06-08 |
| BUG-516 | Replace document.execCommand() with Selection/Range API | v1.115.0 | 2026-06-08 |
| BUG-525 | StoragePlacement unique constraint (shelf_id, slot_index) | v1.115.1 | 2026-06-09 |
| BUG-530 | AppSetting nullable=False on 27 model columns | v1.115.1 | 2026-06-09 |
| BUG-535 | log_movement notes i18n (15+ instances) | v1.115.1 | 2026-06-09 |
| BUG-537 | Models list DB-level pagination (subquery + offset/limit) | v1.115.1 | 2026-06-09 |
| BUG-423 | Monetary fields Float → Numeric(10,2) | v1.116.0 | 2026-06-09 |
| BUG-424 | Keydown listener guard in app-shell.js | v1.116.0 | 2026-06-09 |
| BUG-425 | Context menu listener guard in inventory.js | v1.116.0 | 2026-06-09 |
| BUG-426 | Dashboard drag/resize listener guard + destroy() | v1.116.0 | 2026-06-09 |
| BUG-526 | aria-label on 14+ icon-only buttons | v1.116.0 | 2026-06-09 |
| BUG-527 | x-cloak on flash/undo toasts | v1.116.0 | 2026-06-09 |
| BUG-531 | Restore dropped indexes in waste migration | v1.116.0 | 2026-06-09 |
| BUG-538 | Remove stale TODO(security) comments | v1.116.0 | 2026-06-09 |
| BUG-552 | Subquery for models stats size computation | v1.116.0 | 2026-06-09 |
| BUG-553 | Past due date warning in project create/edit | v1.116.0 | 2026-06-09 |
| BUG-590 | Decimal/Float type mismatch HTTP 500 on /projects and /stats | v1.116.1 | 2026-06-09 |
| BL-005 | Interactive Notification Bell with Live Counter | v1.117.0 | 2026-06-10 |
| BUG-591 | Makerworld link preview broken — Cloudflare 403 + Jina reader opt-in regression | v1.117.1 | 2026-06-15 |
| BL-006 | PostgreSQL Support (4.2) | v1.118.0 | 2026-06-25 |
| BUG-592 | `run_migrations()` idle-in-transaction blocks auto-backup on PG | v1.118.1 | 2026-06-25 |
| BUG-593 | Waste page AJAX endpoints not mapped in `SECTION_BY_ENDPOINT` — 403 on all AJAX interactions | v1.119.1 | 2026-06-28 |
| BUG-594 | Backup export/import missing 7 fields — data loss on restore | v1.119.2 | 2026-06-28 |
| BUG-595 | Heatmap legend shows unresolved `{{T:less}}`/`{{T:more}}` placeholders | v1.119.2 | 2026-06-28 |
| BUG-560 | `.env` not in `.dockerignore` — SECRET_KEY and FERNET_KEY leak into Docker image | v1.119.3 | 2026-06-28 |
| BUG-596 | PostgreSQL `DATETIME` → `TIMESTAMP` compatibility in 11 migration calls | v1.119.3 | 2026-06-28 |
| BUG-597 | SSRF guard for Bambu cover image fetch | v1.119.3 | 2026-06-28 |
| BUG-598 | IDOR bypass for project-less files in `_check_file_access()` | v1.119.3 | 2026-06-28 |
| BUG-599 | N+1 backup export — 7 lazy-load queries per project | v1.119.3 | 2026-06-28 |
| BUG-600 | Markdown code block unreadable in light theme — `[&_code]:bg-gray-100` overrides dark `<pre>` background | v1.119.4 | 2026-07-02 |
| BUG-601 | Alpine.js JS error on project create — `|tojson` quote nesting + `$parent` undefined in `$watch` | v1.119.5 | 2026-07-04 |
| BUG-602 | `/filaments` returns 500 — `strptime()` type mismatch (PG `datetime.date` vs SQLite `str`) | v1.119.5 | 2026-07-04 |
| BL-012 | Bambu: Manual Print Duplicate | v1.119.6 | 2026-07-04 |
| BL-013 | Project tags on model views | v1.119.7 | 2026-07-13 |
| BL-014 | Tag filter on models page | v1.119.8 | 2026-07-13 |
| BUG-603 | Color picker dropdown hidden behind widgets (portal pattern + mousedown fix) | v1.119.12 | 2026-07-13 |
| BL-015 | Bambu waste modal: interactive filament search + AMS auto-suggest | v1.119.10 | 2026-07-13 |
| BL-016 | Bambu waste modal: interactive Project + AJAX submit + job mapping | v1.119.12 | 2026-07-13 |
| BUG-603 | Color picker z-index (portal + mousedown fix) | v1.119.12 | 2026-07-13 |
| BUG-700 | Missing `flash` import in routes/bambu.py | v1.119.13 | 2026-07-18 |
| BUG-701 | Missing `translate` import in routes/auth.py | v1.119.13 | 2026-07-18 |
| BUG-702 | Greedy regex in JSON-LD extraction | v1.119.13 | 2026-07-18 |
| BUG-703 | ICS comma escaping `\:` → `\,` | v1.119.13 | 2026-07-18 |
| BUG-704 | Backup retention `0 or 10` → explicit None check | v1.119.13 | 2026-07-18 |
| BUG-705 | SQLite-incompatible ALTER COLUMN → dialect guard | v1.119.13 | 2026-07-18 |
| BUG-562 | ports mapping in docker-compose.yml | v1.119.14 | 2026-07-18 |
| BUG-563 | prusa_print action type in stats.py (pre-existing fix) | v1.119.14 | 2026-07-18 |
| BUG-564 | XSS via innerHTML in markdown-editor.js | v1.119.14 | 2026-07-18 |
| BUG-565 | invalidate_all_user_sessions commit | v1.119.14 | 2026-07-18 |
| BUG-706 | XSS via |e in onclick → |tojson (21 locations) | v1.119.14 | 2026-07-18 |
| BUG-707 | SSRF bypass via DNS resolution in validate_printer_host | v1.119.14 | 2026-07-18 |
| BUG-708 | Admin self-logout on profile save | v1.119.14 | 2026-07-18 |
| BUG-709 | ORM mutation after rollback in prusa.py | v1.119.14 | 2026-07-18 |

---

## Summary Statistics

### Current State (post v1.119.15 — 2026-07-18)

| Category | Fixed | Remaining |
|----------|-------|-----------|
| 🔴 Critical | 16 | 0 |
| 🟠 High | 20 | 1 (BUG-561) |
| 🟡 Medium | 17 | 11 remaining |
| 🟢 Low | 7 | 8 remaining |
| 🆕 Features | BL-004-007, BL-012-016 | — |
| **Total** | **68 fixed** | **14 remaining** |

### v1.119.0 — UI Enhancement Sprint
- **BL-007** (Implement dark mode / mobile / viz / micro-interaction batch): Closed in v1.119.0. Added 15 new test cases (`tests/test_ui_v119.py`), 4 new assets (`static/css/enhancements.css`, `static/js/enhancements.js`), heatmap data in `routes/stats.py`, animated counters, sparklines, responsive table-to-card transformation, and theme-reactive Chart.js.

### Notes on remaining items from audit #1
- **BUG-423** (Monetary fields → db.Numeric): Low priority. Significant schema change requiring migration. Effort: Medium.
- **BUG-424** (Keydown listener cleanup), **BUG-425** (Context menu listeners), **BUG-426** (Dashboard listeners): Low priority JS memory leak issues. No user-facing impact.

### Notes on audit #2 critical items
- **BUG-500, BUG-501** (UserSession backup crash): Only triggers when UserSession rows exist AND backup/export is invoked. Crash is immediate and unrecoverable.
- **BUG-502** (chart_data XSS): Requires a filament name containing `</script>` — unlikely but exploitable. Fix is trivial (`|tojson`).
- **BUG-503** (RBAC bypass): Currently mitigated because ALL endpoints are mapped in SECTION_BY_ENDPOINT. Risk is future endpoints being added without mapping.

### Compliance Scan Results (post v1.116.0 — 2026-06-09)

| Rule | Description | Result |
|------|-------------|--------|
| 1 | i18n via `t()` / `translate()` (no hardcoded text) | ✅ All known hardcoded strings resolved (BUG-504, BUG-535, BUG-539, BUG-540 fixed) |
| 2 | `_safe_alter()` for new columns | ✅ All migrations present |
| 3 | Flask Blueprints | ✅ All 16 `routes/*.py` use `Blueprint(...)` correctly |
| 4 | `SECTION_BY_ENDPOINT` mapping | ✅ Complete (165+ endpoints mapped) |
| 9 | `<div>` closings inside Jinja loops | ✅ No issues found |
| 12 | Low-stock indicators (`pct` defined first) | ✅ Compliant |
| 15 | Backup schema covers all models | ✅ (BUG-500/501 fixed) |
| 16 | Stats draggable layout | ✅ Compliant |
| 20 | `request.form.get()` (never `request.form[]`) | ✅ Zero hits in production code; `request.files['file']` used but preceded by explicit `'file' not in request.files` check |
| 24 | `utc_now()` (never `datetime.utcnow()`) | ✅ Zero hits in production code |
| 25 | Backend `translate()` follows active language | ✅ All log_movement notes now use translate() (BUG-535 fixed in v1.115.1) |
| 29 | Architecture docs in sync | ✅ Aligned |
| 30 | `HELP_SECTIONS` for new endpoints | ✅ All endpoints covered |
| 31 | Targeted AJAX DOM updates (DOMParser) | ✅ Projects index uses pattern |

---

## Legend

| Criticality | Meaning |
|-------------|---------|
| 🔴 Critical | Must fix immediately — blocking production |
| 🟠 High | Important for next release |
| 🟡 Medium | Planned feature or improvement |
| 🟢 Low | Nice-to-have, no urgency |

| Effort | Meaning |
|--------|---------|
| XS | Trivial — < 1 hour |
| S | Small — < 1 day |
| M | Medium — 1-3 days |
| L | Large — 1-2 weeks |
| XL | Extra Large — > 2 weeks |

---

*Last updated: 2026-07-18 (v1.119.15 — 8 medium bugs fixed, 68 total fixed)*
*Canonical architecture: `.kilo/ARCHITECTURE.md`*
